# OmniFlux Player — יומן פיתוח והעברה

---

## 2026-09-24 — קוד פתוח, README, אתר חדש — 2.0.7

הבקשה: לפרסם לציבור כקוד פתוח (GPL-3.0) בלי שום מפתח, עם README מצולם
ואתר מוצר מקצועי. ריפו ציבורי חדש עם היסטוריה נקייה: `omniflux-player`.
ההיסטוריה הקודמת נשארת בריפו הפרטי.

### בדיקת סודות

כל ערך ב-`.env` נסרק מול כל הקומיטים (`git log -S`), וגם תבניות מוכרות
(AIza…, GOCSPX-…, gsk_…, ghp_…, refresh tokens). **אף סוד לא נכנס מעולם.**
בהיסטוריה הישנה נמצאו רק מזהים לא סודיים של 1.x: תיקיית המאגר, חדר הצ'אט
ומייל הקשר. זו הסיבה להיסטוריה נקייה.

### מה שהכנת הפרסום מצאה

1. **ה-client secret של Spotify נצרב בתוכנה.** בקובץ שמופץ לכולם הוא אינו
   סוד: כל אחד יכול לחלץ ולנצל אותו. היה בשימוש רק בחיפוש client credentials,
   שתוצאותיו ממילא מוצגות רק למחובר. עכשיו החיפוש רץ באסימון PKCE של
   המשתמש, והסוד הוסר מהקוד ומ-`env.d.ts`.
2. **חיפוש המוזיקה ריק למשתמש חדש.** `visibleSearch` הציג תוצאות קטלוג רק
   עם Spotify או Deezer מחוברים. אבל ל-Deezer אין מפתח אפליקציה (הרישום
   סגור), ורוב המשתמשים אינם ברשימת Spotify. מעכשיו Deezer מוצג תמיד,
   כקטע של 30 שניות מסומן.
   **הבדיקה שהייתה אמורה לתפוס את זה דילגה תמיד:** היא בדקה אם Deezer זמין
   ב-fetch מהממשק, שם CORS חוסם אותו. עכשיו היא בודקת מהתהליך הראשי.
3. **Spotify במצב פיתוח = 5 חשבונות** (מדיניות Spotify מפברואר 2026). חשבון
   שאינו ברשימה מתחבר "בהצלחה" ומקבל 403 על כל בקשה. `listed()` ב-
   `spotify-auth.ts` בודק את `/v1/me` מיד אחרי ההתחברות, מנתק, ומציג
   `spotify.unlisted` בשמונה שפות.
4. **צ'יפ "מפתח אפליקציה לא מוגדר" של Deezer** הוסתר כשאין מפתח. למשתמש
   אין מה לעשות איתו.

### קבצים חדשים

`LICENSE` (GPL-3.0), `README.md` + `README.fr.md`, `PRIVACY.md` (נדרש גם
לאימות Google), `SECURITY.md`, `CONTRIBUTING.md`, תבניות issue,
`.github/workflows/pages.yml` (האתר מ-`site/`). `.env.example` נכתב מחדש.
`tools/readme-shots.mjs` מצלם מסכים בפרופיל נקי עם ספרייה לדוגמה, בלי
חשבון מחובר ובלי תוכן אישי. ה-README הטכני הישן עבר ל-`docs/ARCHITECTURE.md`.

### האתר

`site/index.html` נכתב מחדש: **אנגלית וצרפתית** (החלטת המשתמש — השפות העיקריות של המוצר; עברית היא רק אחת משמונה שפות הממשק, ולא מבליטים אותה). כותרת: "Everything you watch and listen to. One app.". סעיף "Films & séries" — חיפוש אחד בכל פלטפורמות הסטרימינג (כלול/השכרה/רכישה), המנויים שלי קודם, טרנדים וטריילרים, הרשימה שלי. בשפה החזותית של התוכנה. כפתור
ההורדה נמשך מהשחרור האחרון. כולל סעיף "מה עובד היום" (Spotify בהזמנה בלבד,
מסך האזהרה של Google בדרייב). נבדק ב-1440 ו-390 בשתי השפות, בלי גלילה אופקית.

---

## 2026-09-23 (לילה) — חיפוש אחוד, מסך מוזיקה מאפס, ובדיקת תקינות חיה — 2.0.6

הבקשה: חיפוש מול כל המקורות עם בורר מקור, מחיקה ובנייה מחדש של מסך
המוזיקה, בדיקת תקינות כוללת לפני כל דבר אחר, ותוכנית למובייל.

### חיפוש אחוד

`src/shared/music-search.ts` מאחד שורות מהמחשב, מהדרייב, מ-Spotify,
מ-Deezer ומ-YouTube לשיר אחד, עם בורר מקור ("נגן מהמחשב / מהדרייב /
מ-YouTube"). YouTube מצטרף בכל הקשה רק מהמטמון (בלי מכסה); חיפוש חי —
ב-Enter. טהור ובדוק, ויעבור כמו שהוא למובייל. פירוט ב-`docs/MUSIC_STRATEGY.md`.

### מסך המוזיקה — מאפס

כותרת וחיפוש אחד; באנר מתחלף (פופולרי מ-YouTube, חדש מ-Deezer); וכל השאר
קרוסלות אופקיות: להמשיך להאזין, בשבילך, הפלייליסטים שלך, מצבי רוח
וז'אנרים, האוסף שלך, יצאו לאחרונה, פופולרי, אמנים, Spotify. נוספו
פלייליסטים מקומיים (לא היו בכלל — "שמירת פלייליסטים" ברשימת הבדיקות
הייתה ריקה).

### בדיקת התקינות מצאה חמש תקלות אמיתיות

`tools/qa/live-regression.cjs` — התוכנה רצה ישירות, בפרופיל מבודד, מול
מד Core Audio. 30 בדיקות. מה שהיא מצאה:

1. **משתמש חדש לא ראה חלון.** ההמתנה לרכיב ה-DRM של Google לפני החלון
   הראשון הייתה בלי גבול. בפרופיל נקי: שלושה תהליכים, אפס חלונות. עכשיו
   עשר שניות לכל היותר; Spotify ממשיך לחכות לרכיב האמיתי.
2. **קובץ פגום תקע את המנוע, והכפתורים מתו.** סרט של 4GB מהדרייב שבו
   ffmpeg מדווח "timescale not set" ומחליט שהקובץ הוא תמונה — וקורא את
   כולו. התהליך חי, ולכן ההתאוששות מנפילה לא הופעלה. עכשיו: שתי פקודות
   שלא נענו → המנוע משתחרר *בלי* הקובץ שתקע אותו, והמשתמש מקבל הסבר.
   אומת: הקובץ פגום גם ב-mpv לבד, ישירות מ-Google.
3. **YouTube המשיך מתחת לסרט** שהתחיל מבחוץ ("פתח באמצעות").
4. **דילוג ב-YouTube בזמן פרסומת נבלע.** עכשיו הוא נשמר ומבוצע כשהשיר
   מתחיל; בזמן הפרסומת הסרגל על אפס ותווית "פרסומת".
5. **Escape בחלון החזיר את כל המסך לבית** — עכשיו שכבה אחת בכל לחיצה.

וגם: קריסה של חלון הממשק נרשמת ביומן והממשק עולה שוב (הסרט ב-mpv ממשיך).

### מה שנראה כמו תקלה ולא היה

"השמע נעלם אחרי כמה שניות" — שוחזר שוב ושוב בבדיקות, עם `volume=0` מ-IPC.
העקיבה הגיעה עד כפתור ההשתקה, ואז רישום לחיצות העכבר הראה לחיצות אמיתיות
(`isTrusted`, `pointerType=mouse`) — מישהו ליד המחשב השתיק את צלילי
הבדיקה. הבדיקה החיה מסמנת עכשיו לחיצות כאלה.

### כלים שנשארו

- `OMNIFLUX_AUDIO_DEBUG=1`, `OMNIFLUX_MPV_LOGFILE=…` — אבחון "אין צליל" אצל משתמש.
- `docs/QA_CHECKLIST.md` — אוטומטי, חי וידני. `docs/MOBILE_ROADMAP.md` — React Native.

### מלכודת

`npx electron-vite build > /dev/null` הסתיר בנייה שנכשלה (שורה חדשה
בתוך מחרוזת), והבדיקות רצו על `out/` הישן — מסקנה שגויה אחת כמעט נכתבה
כך. לבנות תמיד עם הפלט גלוי.

### מצב

- `npm test` — **337 passed**, **2 skipped**. בדיקה חיה: **30/30** (ודילוג אחד — פרסומת שלא נגמרה).

---

## 2026-09-23 (ערב) — YouTube, תור אחוד, וכרטיסי קטגוריות — 2.0.5

המשתמש הוסיף מפתח YouTube Data API, ביקש שהמוצר יהיה לכולם (ולא "לאלף
משתמשים" — זו הייתה הגדרה של הגרסה הישנה), להסיר את מצב השבת, ולעבור
שוב על כל סעיפי הבקשה המקורית. ההחלטות והמגבלות מתועדות ב-
`docs/MUSIC_STRATEGY.md`; כאן מה שנמצא בדרך.

### תקלות שהתגלו תוך כדי

- **כל embed של YouTube הציג "שגיאה 153".** הממשק נטען מ-`file://` ואינו
  שולח Referer. כלומר גם **הטריילרים היו שבורים**. תוקן ב-`youtube-embed.ts`.
- **"נגן הכול" ניגן שיר אחד.** לא היה תור כלל, ו-mpv עם `--keep-open` אינו
  שולח `end-file` לעולם — אף אחד לא ידע ששיר נגמר. עכשיו `eof-reached`.
- **סרגל העוצמה הציג אפס בזמן שהרמקולים ניגנו.** בזמן פרסומת הנגן של YouTube
  מדווח `volume=0, muted=true`. נמצא בעקיבה אחרי מי כותב לחנות; העוצמה של
  המשתמש היא האמת, ומוחלת מחדש בכל מעבר לניגון.
- **הכפתור הציג "נגן" בזמן פרסומת** — "מושהה" הוא רק השהיה, סוף, או מוכן-לא-התחיל.
- **שיר מקומי לא סומן כמתנגן** — `playItem` אינו נושא מזהה; עכשיו לפי התור.

### מה נבנה

- ספק YouTube (`provider-youtube`): חיפוש מוזיקה, "פופולרי עכשיו" לפי אזור,
  פלייליסט מקישור, ניקוי שמות ("Artist - Title (Official Video)"). מכסה משותפת
  → חיפוש בלחיצה, מטמון על הדיסק, 15 חיפושים ביום למשתמש.
- נגן גלוי אחד (`YouTubeDock`): צף 400×225, ובמסך "מתנגן עכשיו" במקום העטיפה
  — אותו iframe, בלי טעינה מחדש.
- תור אחוד (`store/queue.ts`), היסטוריית האזנה ומנוע המלצות מקומי.
- תגיות ID3 ועטיפות מקבצים מקומיים (`music-metadata`, רק לקבצים שהשתנו).
- מסך המוזיקה: האזנה אחרונה, בשבילך, מצבי רוח וז'אנרים ככרטיסי תמונה,
  פופולרי, האמנים שלך, ייבוא פלייליסט, ומגירת תור.
- 16 כרטיסי קטגוריה שנוצרו ב-OpenArt. כרטיס הרוק הראשון יצא עם לוגו של יצרן
  מגברים אמיתי למרות "no logos" — נוצר מחדש. לבדוק כל תמונה בגודל מלא.
- מצב השבת הוסר — שארית של 1.x, לא היה מחובר לשום מסך.

### נמדד חי (הרצה ישירה + CDP + מד Core Audio)

ניגון מ"פופולרי" — צליל מהשנייה הראשונה; "הבא" — השיר השני; קפיצה לסוף — מעבר
אוטומטי לשלישי; "מתנגן עכשיו" — הנגן עובר למקום העטיפה (638×358).

### מצב

- `npm test` — **319 passed**, **2 skipped**.

---

## 2026-09-23 (המשך) — ספוטיפיי בלי צליל, וסרט שאי אפשר להמשיך

שני דיווחים: "השיר משתתק אחרי עשר שניות והטיימר ממשיך", ו"השהיתי סרט
מהדרייב ואי אפשר להמשיך". שניהם נמדדו על התוכנה עצמה בהרצה ישירה,
והמסקנה של כל אחד מהם שונה מההשערה הראשונה.

### הסרט: השהיה נקראה "אין מה לנגן"

השדה `idle` בממשק הגיע מ-`core-idle` של mpv, וזה דלוק גם בהשהיה —
נמדד: `pause=yes core-idle=yes`. הממשק הסיק "אין קובץ", נעל את כפתור
הניגון (`disabled={idle && paused}`), ו-`togglePlayback` חזר בלי לעשות
דבר. זה לא היה ייחודי לדרייב: כל קובץ שהושהה ננעל כך.

עכשיו `idle` הוא `idle-active` — "אין קובץ טעון" — ובחנות נוספה בדיקת
`paused`, כדי שסרט מושהה לא ייקח את הבמה מ-Spotify.

הבדיקה הקיימת לא תפסה את זה כי קראה למנוע ישירות ב-IPC, מעל השכבה
שנשברה. הבדיקה החדשה משהה וממשיכה מהמקלדת; בלי התיקון היא נכשלת
בדיוק על ההמשך. אומת גם מול דרייב אמיתי: סרט של 2.2 ג'יגה, השהיה
של 25 שניות, והמשך מאותה נקודה.

### ספוטיפיי: אין רישיון, ולכן אין צליל — בכלל

המדידה (CDP לתוכנה שרצה ישירות, עם האזנה בתוך ה-iframe של ה-SDK):
‏Widevine נבחר, ה-CDM מייצר בקשת רישיון תקינה, ו-
`api.spotify.com/v1/widevine-license` עונה **500** על כל בקשה בפיתוח,
ו-**403** בגרסה 2.0.3 המותקנת. בלי מפתח, השמע המוצפן יורד (20 שניות
בחוצץ, `readyState=4`) ולעולם אינו מתנגן.

‏`castlabs_evs.vmp verify-pkg` מסביר את שני המספרים:

- ‏Electron מ-node_modules: "Certificate is valid for development only".
- החבילה המותקנת: "InvalidSignature". electron-builder משנה את שם
  `electron.exe` ל-`OmniFlux Player.exe`, והקובץ `electron.exe.sig`
  שהגיע מ-CastLabs שייך לקובץ שכבר לא קיים.

"עשר שניות" לא היו צליל אמיתי. אחרי הדחייה ה-SDK מדווח לרגע "מנגן"
עם מיקום חיובי, וה-Web API מקדם את המיקום בעצמו — הסרגל זז, ושקט.

מה השתנה:

- **`tools/vmp-sign.cjs`**, כ-`afterSign` של electron-builder: חתימת VMP
  לייצור ואז אימות. בנייה שהאימות שלה נכשל **נעצרת**; שחרור בלי חתימה
  הוא Spotify שבור אצל כל המשתמשים. דורש חשבון EVS (חינם) — הגדרה
  חד-פעמית בראש הקובץ. `OMNIFLUX_SKIP_VMP=1` לבניית בדיקה בלבד.
- **זיהוי הדחייה בשרת הרישיונות**, ב-`webRequest` של הסשן (הבקשה
  יוצאת מה-iframe). שתי דחיות רצופות מכריעות, וההתאוששות עוצרת מיד.
  נמדד: הודעה ברורה כחמש שניות אחרי הלחיצה, במקום עשרים שניות של
  השהיה-והמשך — שלפעמים ויתרו על החלופה בכלל, כי ה-SDK "ניגן".
- **דחייה גוברת על "מנגן"** של ה-SDK, גם בהתאוששות וגם ב-
  `markPlaybackFailed`.
- **‏`linked_from`**: שיר שספוטיפיי החליפה בגרסה מקבילה נחשב השיר שלנו,
  ולא נקטע כל שנייה וחצי.
- הלחיצה הבאה אחרי דחייה מציגה את הסבר ה-DRM ולא "נכשל".

### שאר המקורות — נבדקו חי, תקינים

קובץ שמע מקומי (40 שניות, רציף), תצוגה מקדימה של Deezer (30 שניות
מלאות), וסרט מדרייב (פריים ראשון אחרי 3 שניות).

### מלכודת

‏`npm test` אינו בונה: Playwright מריץ את `out/`. הריצה הראשונה של
הבדיקה החדשה נכשלה עם התיקון ובלעדיו — כי היא בדקה בנייה ישנה.

### מצב

- `npm test` — **307 passed**, **2 skipped**.
- חתימת VMP ממתינה לחשבון EVS; עד אז `npm run dist` ייעצר, בכוונה.

### המשך: החתימה עבדה, וחשפה תקלה שנייה — ושוחרר 2.0.4

המשתמש פתח חשבון EVS. החבילה נחתמה ואומתה, ושרת הרישיונות ענה 200:
`license-ok`, `keystatus:usable`. ועדיין — שקט. מד העוצמה של Windows
(Core Audio, לכל תהליך; אומת קודם מול mpv) לא ראה את OmniFlux בכלל.

**‏state_conflict.** ‏`play()` שלח קודם העברה (`PUT /me/player`) ומיד
אחריה ניגון. שתי הפקודות הגיעו למכשיר כמעט יחד; ה-SDK קיבל
`state_conflict` שלוש פעמים, איפס את הנגן (`emptied`), ומאז לא הוריד
ולא הזין אף בית (`appendBuffer` לא נקרא). הרישיון תקף, הסרגל רץ, אין
צליל.

שני ניסויים הכריעו: כיבוי ההתערבות של ההתאוששות לא שינה דבר (היא לא
הופעלה כלל, וההתנגשות קדמה לה); ניגון בלי ההעברה — צליל תוך שנייה.
עכשיו: ניגון עם `device_id` בלבד, והעברה רק כשהוא עונה 404.

נמדד בבנייה החתומה, מול הרמקולים: צליל רציף 57 שניות (שיא 0.3–0.77),
השהיה מהמקלדת — 0.000, המשך — הצליל חוזר.

### החתימה בפועל

- ‏EVS מעלה את כל קובץ ההפעלה (225 מגה) בכל חתימה. כאן: 0.6 מגה בשנייה,
  כשש דקות וחצי. ריצה אחת נכשלה ב-`ExpiredToken` באמצע ההעלאה —
  ה-hook מנסה עכשיו עד שלוש פעמים, כל פעם עם חריץ העלאה חדש.
- פורט 9333 נפל בטווח ש-Windows שומר (`excludedportrange`) — ‏CDP לא
  עלה. לאבחון: פורט מחוץ לטווחים האלה.

### מצב

- `npm test` — **307 passed**, **2 skipped**.
- `v2.0.4` בשני הערוצים.

---

## 2026-09-23 — שלוש תקלות שהתחברו לאחת, והקלטת מסך שפתרה אותן

המשתמש צילם את המסך: הוא בוחר שיר, התוכנה קופצת לשיר אחר, אחר כך
לשלישי, כולם על 0:00, בלי צליל, ולחיצה על השהיה לא עושה כלום. ההקלטה
שווה יותר מכל תיאור — ממנה נגזרו שלושת השורשים.

### 1. המשטח הנסתר נטען מ-file://

הניגון של Spotify ו-Deezer רץ בדף נסתר שאנחנו מריצים. דף שנטען
מקובץ הוא "מקור אפס", ובקשות המדיה של ה-SDK נדחות אז ב-CORS —
נמדד בפועל: `Access to fetch at 'https://api.spotify.com/v1/track-playback/…'
has been blocked by CORS policy`. כל שיר נכשל בטעינה.

**זה לא נראה אף פעם בפיתוח**, כי שם הדף מגיע משרת Vite — כלומר
ממקור אמיתי. רק המשתמש הארוז נפגע, וזה בדיוק סוג התקלה שבדיקות על
בניית הפיתוח לא יתפסו לעולם.

`src/main/services/surface-origin.ts` מגיש עכשיו את שני המשטחים
מ-127.0.0.1: מקור אמיתי, והקשר מאובטח לפי התקן. רק תיקיית הממשק
הבנויה מוגשת, רק סיומות מוכרות, ואסימון אקראי בנתיב.

### 2. הרשימה הפכה כישלון רגעי לדילוג

לספוטיפיי נשלח השיר שנבחר ואחריו כל מה שמתחתיו ברשימה. כישלון
בהתחלת ניגון מטופל אצלה כמו סוף שיר: מעבר לבא בתור. וכך, בלחיצה
אחת, חמישה שירים התחלפו לבדם.

עכשיו נשלח שיר אחד בלבד — אין לאן לדלג — ומאחוריו התאוששות שמודדת
אם המיקום באמת מתקדם, ומתערבת רק כשהוא תקוע. ההתערבות היא השהיה ואז
המשך; שתי חלופות נמדדו ונדחו: "המשך" לבדו (24 בקשות ברצף לא הזיזו
כלום) ופקודת ניגון חדשה (שלושים שניות בלי צליל, כי כל בקשה מתחילה
את הטעינה מחדש).

הוספה לתור של ספוטיפיי נוסתה ונדחתה גם היא: התור נשמר בחשבון גם
אחרי שהניגון הוחלף, ובפעם הבאה שהניגון נכשל לרגע — דילוג אל שאריות
התור. כלומר אותה תקלה, רק מהצד השני.

### 3. ‏activateElement הרג את הנגן

הוא נועד לעקוף חסימת הפעלה אוטומטית בעזרת מגע של המשתמש, ובמשטח
נסתר אין מגע. הכישלון שלו פלט אירוע פנימי — `ACTIVATE_ELEMENT_ERROR` —
שאיש לא נרשם אליו, ו-`_getListeners` של ה-SDK עושה `__spreadArray`
על `undefined`. כלומר לולאת ההודעות של ה-SDK מתה: `ready` לעולם לא
הגיע, הנגן שלנו לא נרשם כמכשיר, והניגון נשלח למכשיר אחר בחשבון.

במקומו: `autoplayPolicy: 'no-user-gesture-required'` בחלון עצמו,
והגנה שמוודאת שאירוע פנימי לא מוכר לא יפיל שוב את הנגן — הגנה על
התשתית, לא על שם אירוע מסוים, כי השמות הפנימיים אינם מתועדים.

### והווידאו: מנוע שנפל נשאר מת

‏mpv הוא תהליך נפרד, ותהליך יכול למות. עד היום התוכנה רק הודיעה על
כך — ומאותו רגע כל לחיצה נשלחה לצינור שאין בצדו איש. זה בדיוק
"לוחץ המשך ולא קורה כלום".

עכשיו הוא קם מחדש וחוזר לאותו קובץ ולאותה שנייה. בדרך התברר ששם
הצינור היה חלק מהבעיה: הוא נגזר ממזהה התהליך של התוכנה ולכן היה
קבוע, ומנוע חדש נקשר לצינור הישן והמת — הפקודות נשלחו לשומקום וחזרה
שגיאת "הפקודה לא נענתה" אחרי שמונה שניות. עכשיו שם חדש בכל הפעלה,
והתאוששות אחת בכל רגע.

### ובממשק

אחרי שמיעת שיר ב-Spotify, כל דיווח של המנוע המקומי נזרק — לתמיד.
מי שפתח אחר כך סרט ראה סרגל נגן של השיר הישן, והכפתורים שלו נשלחו
ל-Spotify במקום לסרט. קובץ שמתנגן כאן מחזיר לעצמו את הבמה.

וכשהניגון התבקש ועדיין לא נשמע, הכפתור הראשי מציג סימן טעינה ואינו
מקבל לחיצות. ההתחלה הראשונה של ספוטיפיי לוקחת בין שנייה לעשר, וזו
התחלה קרה שלה ולא משהו שבשליטתנו: נוסו מקור https עם תעודה מקומית
(אותו זמן), ביטול חלוקת עוגיות צד-שלישי (אותו זמן), והמתנה של
שתים-עשרה שניות לחימום המכשיר (לא שינתה דבר).

### שיטה

כל אבחון כאן נעשה על התוכנה עצמה בהרצה ישירה, ולא דרך Playwright.
זה לא היה מובן מאליו: דרך Playwright נמדד ש-Widevine אינו זמין
כלל — מסקנה שהייתה שולחת לחתימת VMP מיותרת — ובהרצה ישירה של אותה
בנייה בדיוק הוא זמין. כלי בדיקה הוא חלק מהמערכת הנמדדת.

### מצב

- `npm test` — **304 passed**, **2 skipped**.
- בדיקות חדשות: התאוששות המנוע (הריגה אמיתית של התהליך באמצע סרט),
  והמקור המקומי (הגשה, אסימון, ויציאה מהתיקייה).

---

## 2026-09-22 (המשך) — מעבר מהגרסה הישנה, ושחרור 2.0.1

הגרסה 2.0.0 שוחררה, ומיד אחר כך התגלה שהיא מפילה את מה שהמשתמשים
בנו. המתקין מסיר את "בית הקולנוע", אבל הגרסה החדשה שומרת את הנתונים
שלה בתיקייה אחרת לגמרי — כלומר כ-1,000 משתמשים היו נפתחים לספרייה
ריקה. "מעבר מהגרסה הישנה" שנרשם בסבב הקודם היה ההסרה בלבד, ולא
העברת נתונים.

מה עובר: `rootFolderId` ו-`userFolders` מתוך `settings.json` של 1.x,
אל רשימת המקורות, עם השם שהמשתמש ראה ועם תאריך ההוספה המקורי. מה
שלא עובר בכוונה: הקטלוג והמטא-דאטה — המבנה שלהם שונה, והסריקה בונה
אותם מחדש נכון. ההסרה אינה מוחקת את נתוני הגרסה הישנה
(`deleteAppDataOnUninstall: false`), ולכן הקובץ שם כשצריך אותו.

רץ פעם אחת ומסומן בקובץ: תיקייה שהמשתמש הסיר אינה חוזרת. רץ בבנייה
ארוזה בלבד — במחשב פיתוח שיש עליו את הגרסה הישנה הוא הזליג את
התיקיות שלה לכל הרצת בדיקות, ושתי בדיקות ספרייה אכן נפלו כך.

‏10 בדיקות על הפורמט האמיתי של 1.x, ואימות מקצה לקצה מול ההתקנה
החיה של 1.6.1 במחשב הזה: התיקייה "סרטים" עברה עם שמה ועם תאריכה.

### גם כאן

בדיקת הארוז בחרה את תהליך ה-mpv הראשון במערכת, ולכן דיווחה שהארוז
שבור כשמנוע אחר רץ במקרה — ובסוף סגרה כל עותק של התוכנה במערכת, כולל
הסרט שהמשתמש צופה בו. עכשיו היא מזהה את המנוע לפי מזהה התהליך
שפתחה, וסוגרת רק אותו.

### שוחרר

- `v2.0.1` בשני הערוצים: `omniflux-player-releases` ו-`beit-hakolnoa-releases`
  (הערוץ שממנו מתעדכנת הגרסה הישנה).
- `v2.0.0` סומנה כמוחלפת בשני הערוצים — היא שוחררה בלי המעבר.
- `npm test` — **298 passed**, **2 skipped**. בדיקת הארוז עוברת.

---

## 2026-09-22 — ניגון מדרייב שעובד, שפת הטריילר, וסבב עיצוב לפי מדידה

הסבב הזה נפתח בדיווח אחד: "ניגון מדרייב לא עובד בכלל". הוא נכון,
והסיבה לא הייתה באג אחד אלא בשתי החלטות ארכיטקטוניות.

### למה דרייב לא ניגן

**חשבון Google היה תנאי.** הדרך הראשית להוסיף דרייב היא הדבקת קישור
לתיקייה משותפת — וזה בדיוק המסלול שלא ניגן: הקטלוג סרק את התיקייה עם
מפתח ה-API, הכותרים הופיעו בספרייה, ולחיצה עליהם דרשה חשבון מחובר.
מפתח API מספיק לכל תיקייה שמשותפת "לכל מי שיש לו הקישור"; האסימון
נשאר כשהוא קיים, כי בקשה מזוהה נזקפת למשתמש ומגיעה גם לקבצים פרטיים.

**מקטעים של 64 קילובייט.** כל משתמשי התוכנה חולקים פרויקט Google Cloud
אחד — מפתח אחד ולקוח OAuth אחד צרובים בקובץ ההפעלה — ולכן גם מכסה
אחת. סרט של 2 ג'יגה היה כ-32,000 קריאות API, ומקטע של 64 קילובייט עם
השהיה של כ-100 מילישניות מגביל את הקצב לכ-5 מגה-ביט: פחות ממה
ש-1080p צריך. זה מסביר גם את הגמגום כשהניגון כן התחיל, וגם למה
המשתמשים הפילו זה לזה את המכסה.

### המנוע החדש

`src/main/services/drive-engine.ts` — שרת HTTP על 127.0.0.1 ש-mpv מנגן
ממנו. כל בקשת Range מתורגמת לבלוקים קבועים של 8 מגה, כל בלוק יורד
פעם אחת ונשמר על הדיסק. אותו סרט של 2 ג'יגה: כ-256 קריאות במקום
32,000 — **פי 125 פחות**. דילוג אחורה, צפייה חוזרת, והקריאה שקוראת
את אינדקס ה-moov בסוף הקובץ אינם מורידים שוב דבר.

מה שנמדד ולא שוער:

- **מקביליות מזיקה.** שמונה בקשות במקביל (ניסיון להאיץ את הפריים
  הראשון) הפכו קובץ של 21 מגה מ-3 בקשות ו-4 שניות ל-48 בקשות
  ו-11 שניות: גוגל הגיב על הפרץ כעל חריגה ממכסה, והמנוע נפל למסלול
  של טווחים קטנים. התקרה נקבעה לשתי בקשות, וגם קריאה קדימה ממתינה
  בתור.
- **דף ה-"Sorry..." אינו שגיאת הרשאה.** ‏403 עם HTML במקום JSON, שנצפה
  בפועל אחרי אותו פרץ. הוא סווג כחסימת גישה, והמשתמש היה מקבל עצה
  לחבר חשבון לבעיה שנפתרת מעצמה תוך דקות. עכשיו: "עמוס, נסו שוב",
  בלי ניסיון חוזר שרק מאריך את החסימה.
- **הפריים הראשון לא צריך בלוק שלם.** המנוע המתין ל-8 מגה לפני שהעביר
  בייט אחד; בקובץ אמיתי זה 6.4 שניות. כל בלוק נכתב עכשיו לחוצץ תוך
  כדי ההגעה, והבייטים עוברים לנגן מיד: **1.7 שניות**, אותן שלוש
  בקשות בדיוק.
- **חיבור שנופל** ממשיך מהבייט האחרון שהגיע, והזמן הקצוב חל על הפסקה
  בזרימה ולא על הבקשה כולה.

מול דרייב אמיתי, בלי חשבון מחובר: קובץ מתנגן ב-mpv, **3 בקשות נתונים,
0 ניסיונות חוזרים**. ‏16 בדיקות מול שרת Google מזויף מכסות טווחים
מדויקים, ‎416/HEAD‎, חיסכון בבקשות, מטמון ששורד הפעלה מחדש, איחוד
בקשות לאותו בלוק, נפילה לטווחי משנה במכסה, ‏429, קובץ פרטי, ודף
ההגנה.

### שפת הטריילר

טריילר נפתח תמיד באנגלית, גם בממשק בצרפתית. ‏TMDB מחזיק וידאו לפי
שפה, והבקשה לא ביקשה. עכשיו בקשה אחת מביאה את שפת הממשק ואת האנגלית,
והבחירה מדורגת: שפת הממשק, רשמי, טריילר לפני טיזר, החדש ביותר. כשאין
טריילר בשפה, נפתחת אנגלית עם כתוביות בשפת הממשק מופעלות מראש.

נמדד מול TMDB חי, שמונה כותרים לשפה: צרפתית 8/8 מקורי, ספרדית 8/8,
גרמנית 7/8, איטלקית 7/8, פורטוגזית 5/8, עברית 2/8, ערבית 0/8 — והשאר
מקבלים כתוביות.

### תוכן מקומי בלבד

מתג בהגדרות שמכבה את הרשת: בלי באנר, בלי טרנדים, בלי קטלוג מוזיקה,
והחיפוש אינו יוצא החוצה. הכרזות והתקצירים של הקבצים שכבר בספרייה
נשארים.

### סבב עיצוב — שלוש מסגרות ביקורת על המסכים עצמם

**הכפתור הלבן הראשי היה מת.** כותר חדש שאינו בספרייה ואינו בשום
שירות הציג "פתח" מושבת ב-40% שקיפות — הכפתור הבולט ביותר במסך
הראשון. עכשיו הוא יורד בסולם עד שהוא מוצא פעולה אמיתית: נגינה,
פתיחה בשירות, טריילר, פרטים.

**התפר מתחת לבאנר.** הבאנר הסתיים בקרקע אטומה ומתחתיו הציץ פס של
זוהר האווירה. התמונה נמוגה עכשיו במסכה ולא בצבע. נמדד על שורות
הפיקסלים: המדרגה הגדולה ביותר בין שורות סמוכות — 1.11 בצרפתית,
1.24 בעברית, 0.89 בערבית.

**תנועה.** ‏motion/react רץ ב-JavaScript ואינו רואה את
`prefers-reduced-motion` — מי שכיבה תנועה ב-Windows המשיך לראות הכול.
פסי ההתקדמות הנפישו רוחב, כלומר חישוב פריסה כמה פעמים בשנייה לאורך
כל הנגינה; כולם עברו ל-`scaleX`. ‏25 מופעי `transition-all` הוחלפו
ברשימת מאפיינים מוגדרת.

**נגישות.** קורא מסך הכריז על כרטיס "7.4, כפתור" במקום שם הכותר, כי
`title` אינו נחשב שם כשיש תוכן. נקודות הבאנר היו מטרות של 16 על 3
פיקסלים. חמישה שדות סומנו בטקסט-מציין בלבד. סריקה חוזרת: אפס שדות
בלי תווית, אפס כפתורים בלי שם, `h1` בכל מסך, וניגודיות של 142
אלמנטים בלי אף אחד מתחת ל-WCAG AA.

**וריאנטים של שירותים.** כותר הציג שמונה תגים לארבעה שירותים —
"Amazon Prime Video" לצד "…with Ads". וריאנט יורד עכשיו רק כשהבסיס
שלו באותה רשימה. באותה הזדמנות: מנוי ל-"Netflix" מסמן "כלול" גם כותר
שזמין רק ב-"Netflix Standard with Ads".

### תמונה בתוך תמונה

החלון נשאר לפעמים בגודלו המלא. ‏`setResizable(false)` מקפיא ב-Windows
את הגבולות שקיימים באותו רגע, ו-`setBounds` חוזר לפני שמנהל החלונות
סיים. ההקפאה ממתינה עכשיו שהגודל יוחל — בבדיקה ולא בדחיפה: קריאה
חוזרת ל-`setBounds` בזמן ששינוי כבר בדרך מבטלת אותו ומתחילה מחדש,
וברצף מהיר החלון נשאר לתמיד גדול.

### ניקיון

- ‏18 מפתחות תרגום שאיש לא קרא להם — מסך פתיחה שהוסר, כפתורי חלון
  שהוחלפו — כלומר 144 מחרוזות בשמונה שפות. הוסרו, ונוספה בדיקה
  שמונעת את הבאים.
- ‏`legacy_services/drive-fetch/` הוסר: 407 שורות של המשיכה במקטעים
  של מגה שהמנוע החדש החליף, והמודול היחיד שעוד כתב לדיסק בעצמו.

### מצב

- `npm test` — **288 passed**, **2 skipped**, 0 failed.
- `npm run typecheck` — נקי. `npm run build` — נקי.
- ניגודיות: 142 אלמנטים, 0 מתחת ל-WCAG AA, הנמוך ביותר 5.12:1.

---

## 2026-08-25 — ביקורת מוכנות: סמל התוכנה, וביטול חסם שחרור שגוי

### הסמל — הבאג שהמשתמש דיווח עליו

הסמל אכן נבנה מחדש בכל אריזה, אבל מה שנבנה היה כתם אפור חיוור ולא
הסימן. הסיבה נמדדה ולא שוערה: קובץ הקונספט
`docs/brand/omniflux-mark-concept-v1.png` **אינו אטום באף פיקסל שלו**.
ערוץ האלפא נע בין 126 ל-236 על פני כל הקנבס, והרקע הכהה הוא
`rgba(4,4,9,189)` — שחור ב-74% אטימות בלבד.

`tools/_makeicon.mjs` העתיק את הקובץ כמו שהוא אל סימן הממשק, וצילם
אותו עם `omitBackground` אל `build/icon.png`. כך שרדה השקיפות החלקית
עד קובץ ה-ICO. ‏Windows מרכיב סמל מעל משטח בהיר — סייר הקבצים, המתקין,
שורת המשימות — ורקע שחור ב-74% אטימות הופך שם לאפור. נבדק בפועל על
המסגרת בגודל 256 שחולצה מ-`release/.icon-ico/icon.ico`.

הכלי נכתב מחדש ומייצר שני תוצרים מאותו מקור יחיד:

- **סימן הממשק** — הרקע הכהה מוסר לפי בהירות ונשאר הגליף בלבד על
  שקיפות אמיתית. נמדד: 86.9% מהפיקסלים שקופים לחלוטין, 13.0% אטומים
  לחלוטין, 0.1% במעבר.
- **סמל התוכנה** — הגליף מורכב מעל לוח אובסידיאן אטום עם פינות
  מעוגלות. נמדד: 74.3% אטומים, 25.6% שקופים בפינות בלבד, 0.0% חלקיים.

ההבחנה שהוחמצה קודם: שקיפות בפינות היא נכונה ומקובלת בכל סמל מודרני.
מה ששבר היה תוכן שקוף-למחצה.

### חסם השחרור שנרשם בסבב הקודם — אינו קיים

בסבב הקודם נרשם ש-`tools/_packagecheck.mjs` נכשל, ושהחבילה אינה מפעילה
את מנוע mpv. הבדיקה הורצה כאן שוב על אותה בנייה בדיוק ועברה במלואה:
המנוע רץ מ-`resources`, הקובץ נטען, הנגינה מתקדמת, ופענוח החומרה
`d3d11va` פעיל.

הכשל שוחזר ואובחן: הכלי המתין 12 שניות קבועות ואז בדק. ההרצה הראשונה
אחרי אריזה איטית בהרבה מהשאר, כי Windows סורק קובץ הפעלה חדש בנפח
236 מגה-בייט לפני שהוא מרשה לו לרוץ. נמדד: המנוע עולה אחרי 10.4 שניות
— בדיוק על גבול הסף. ההרצה השנייה, על קובץ שכבר נסרק, עברה תמיד.

לכן **אין חסם שחרור מצד חתימת CastLabs/VMP**. ההמתנה הוחלפה בהמתנה
למנוע עצמו עד 60 שניות, כך שהבדיקה דטרמיניסטית — וגם מהירה יותר כשהכול
תקין.

הערה מתודית: אבחון שגוי כאן עלה בהכרזה על חסם שחרור שלא היה קיים.
בדיקה שמודדת שעון במקום למדוד תוצאה תייצר את זה שוב.

### גרסה

הגרסה הועלתה ל-`2.0.0-alpha.4`. הסיבה: `v2.0.0-alpha.3` כבר פורסם
ב-20 באוגוסט עם בינארי שונה (137,028,952 בתים), והבנייה הנוכחית שוקלת
147.9 מגה-בייט. פרסום תוכן אחר תחת אותו מספר גרסה משאיר את `latest.yml`
המפורסם עם SHA-512 שאינו תואם, ומשתמש שמותקנת אצלו alpha.3 לא יקבל
עדכון כלל.

### קבצים ששונו

- `tools/_makeicon.mjs` — נכתב מחדש: הסרת רקע לפי בהירות, לוח אטום,
  ופלט נפרד לממשק ולסמל התוכנה.
- `tools/_packagecheck.mjs` — המתנה למנוע במקום המתנה לשעון.
- `build/icon.png`, `build/_icon-preview/*`, `site/assets/icon.png`,
  `src/renderer/src/assets/omniflux-mark.png` — נוצרו מחדש.
- `package.json` — גרסה `2.0.0-alpha.4`.

### בדיקות ואימות

- `npm run typecheck` — עבר ללא שגיאות.
- `npm test` — **253 passed**, **2 skipped**, 0 failed מתוך 255.
- `npm run verify:contrast` — **156 אלמנטים**, 0 מתחת ל-WCAG AA; יחס
  מינימלי `5.08:1`.
- `npm run build` — עבר.
- `npx electron-builder --win` — נוצר
  `release/OmniFlux-2.0.0-alpha.4-setup.exe`.
- `node tools/_packagecheck.mjs` — **עבר**; המנוע עלה אחרי 10.4 שניות.
- קובץ ה-ICO נבדק ישירות: 7 גדלים מ-16 ועד 256, והמסגרת הגדולה חולצה
  ואומתה חזותית.

### מה שנותר פתוח לפני פרסום

1. **המתקין אינו חתום דיגיטלית (Authenticode).** אינו חסם טכני — כל
   שלוש גרסאות האלפא שפורסמו אינן חתומות, והערות השחרור מסבירות את
   אזהרת SmartScreen. חתימה תידרש לגרסה ציבורית יציבה.
2. **תג "Built by Alexandre Heymann"** מוטמע בתחתית סרגל הצד — כלומר
   נראה בכל מסך — וגם במסך ההגדרות. זהו ייחוס פומבי לאדם שאינו בעל
   הריפוזיטורי, והוא טעון אישור מפורש לפני פרסום. הצבע הציאני שבו גם
   אינו חלק מלוח Obsidian.
3. **מטמון הסמלים של Windows.** גם עם סמל תקין, התקנה חוזרת מעל
   אותה תיקייה מציגה לעתים את הסמל הישן עד שהמטמון מתרענן.


## 2026-08-23 — ליטוש Obsidian, נכסים חזותיים ואימות

### פעולות שבוצעו

- מופה מבנה הפרויקט, רכיבי המעטפת, מסך הבית, מערכת הכרטיסים, מנגנון ה־i18n ותשתית הבדיקות.
- גובה ה־Hero ומצב השלד שלו כוילו ל־`clamp(420px, 68vh, 680px)`.
- הוחל Gradient Scrim עמוק ורב־שלבי, כולל אזור המסה מוגדל בתחתית, כדי לשמור על קריאות הטקסט ולמזג את האמנות אל קנבס `#08090C`.
- ריחוף כרטיסי המדיה כויל ל־`scale(1.05)` ללא שינוי מידות או מיקום בפריסה, עם ההילה המבוקשת: `0 0 24px -4px rgb(125 107 240 / 0.35)`.
- נשמר רדיוס הכרטיסים האחיד `--radius-o-lg: 12px` שכבר היה קיים בשפת העיצוב.
- רוחב ה־Sidebar כויל ל־`240px`; ריווחיו הפנימיים, המרווח בין יעדי הניווט והמרווח מתחת למותג הוגדלו.
- גובה ה־MiniPlayer כויל ל־`80px`; הריווח האופקי וה־padding עודנו למראה מרווח ונקי.
- נוצרו ושולבו שני נכסי bitmap מקוריים באמצעות כלי ImageGen המובנה:
  - Hero קולנועי רחב: שכבות זכוכית Obsidian, תאורת Violet/Cyan ומרחב שלילי לטקסט.
  - מרקם fallback אנכי ביחס `2:3`: מרכז כהה לקריאות כותרות עם מונולית זכוכית והילת Violet מרוסנת.
- הסמלים נשארו במערכת הווקטורית הקיימת במקום להחליפם בתמונות raster. החלטה זו שומרת על חדות, נגישות, שינוי גודל והתאמה עקבית ל־RTL.
- הופקו צילומי QA למסכי הבית, הספרייה, ההגדרות והחיפוש בצרפתית, עברית וערבית תחת `.shots/` לצורך בדיקה חזותית מקומית.

### קבצים שנוצרו

- `src/renderer/src/assets/hero-v2.png` — רקע Hero חדש שנוצר ב־ImageGen.
- `src/renderer/src/assets/placeholder-v2.png` — רקע fallback חדש לכרטיסי מדיה שנוצר ב־ImageGen.
- `CHANGELOG_DEV.md` — מסמך זה.
- `.shots/fr-*.png`, `.shots/he-*.png`, `.shots/ar-*.png` — תוצרי QA מקומיים שנוצרו מחדש על ידי `npm run verify:shots` (תיקייה מוחרגת מ־Git).

### קבצים ששונו

- `src/renderer/src/styles/app.css` — התנהגות scale והילת ריחוף של כרטיסים.
- `src/renderer/src/screens/Home.tsx` — נכס Hero חדש, גובה `68vh` ו־scrim עמוק.
- `src/renderer/src/components/Sidebar.tsx` — רוחב וריווחים.
- `src/renderer/src/components/MiniPlayer.tsx` — גובה וריווחים.
- `src/renderer/src/components/Poster.tsx` — שימוש ב־fallback החדש.
- `src/renderer/src/components/TitleCard.tsx` — שימוש ב־fallback החדש.
- `.gitignore` — החרגת צילומי האימות המקומיים תחת `.shots/`.

### קבצים שנמחקו

- לא נמחקו קבצים. הנכסים הקודמים נשמרו כדי לא לשבור צרכנים קיימים ולאפשר השוואה או rollback.

### החלטות UI וארכיטקטורה

- אפקט הריחוף נשאר מבוסס `transform`, ולכן אינו מפעיל reflow ואינו מזיז כרטיסים שכנים.
- הסגול נשאר מוגבל למצבי מיקוד, בחירה, התקדמות והילת ריחוף; פעולות ראשיות נשארו Arctic White.
- שכבות ה־Hero שומרות על כיוון לוגי באמצעות `useDir`, בעוד הרקע החדש תוכנן עם מרחב כהה רחב כך שיתאים גם ל־LTR וגם ל־RTL.
- הנכסים החדשים קיבלו שמות גרסה (`*-v2.png`) ולא דרסו את קובצי המקור.
- פרומפט ה־Hero ביקש רינדור 3D קולנועי 16:9 של זכוכית Obsidian, תאורת Violet/Cyan, מרחב שלילי לטקסט וללא טקסט/לוגו/דמויות.
- פרומפט ה־fallback ביקש מרקם 2:3 כהה, ניטרלי־ז'אנר, עם מרכז רגוע וקריא, ללא טקסט/לוגו/אובייקטים מזוהים.

### תמונת מצב סופית — בדיקות ועבודה תקינה

- `npm run typecheck` — עבר ללא שגיאות.
- `npm test` — עבר: **214 passed**, **2 skipped**, ללא כשלים. שתי הבדיקות המדולגות תלויות בתוכן חיצוני של Deezer/שורת מוזיקה ואינן רגרסיה.
- `npm run verify:shots` — עבר; הופקו 12 צילומים בצרפתית (`ltr`), עברית (`rtl`) וערבית (`rtl`).
- `npm run verify:contrast` — עבר; 52 אלמנטים נמדדו, 0 מתחת ל־WCAG AA, יחס מינימלי `6.59:1`.
- `npm run build` — עבר; main, preload ו־renderer נבנו בהצלחה, כולל שני הנכסים החדשים.
- בדיקה חזותית ידנית של צילומי הבית בצרפתית ובעברית אישרה Sidebar מתהפך, תוכן Hero מיושר לוגית, scrim קריא ושורות כרטיסים תקינות.

### מצב למסירה

היישום בנוי ועובר את סבב הבדיקות המלא. הליטוש המבוקש, נכסי ה־fallback וה־Hero, WCAG AA ו־RTL שולבו ואומתו. לא בוצעו שינויים בתהליך הראשי, ב־IPC, במנוע `mpv` או בחוזי הנתונים.

---

## 2026-08-23 — Master Handover: Music Hub, מיתוג ו־Spotlight Search

### תוצאה מוצרית

- נוסף מתחם מוזיקה עצמאי ברמת ניווט ראשית, ולא רק שורת תוכן במסך הבית.
- נוצר Hero מוזיקלי קולנועי עם גלי זכוכית, מסנני mix מהירים, חיפוש שירים ואלבומים, תצוגות מקדימות של Deezer וחיבור Spotify Connect.
- מוזיקה מקומית מהקטלוג מופיעה במתחם וניתנת לניגון ישיר דרך `mpv`.
- מסך Now Playing שודרג בעטיפה גדולה יותר, שכבת אמביינט הנגזרת מהעטיפה, פאנל מילים מזכוכית וסרגל waveform מונפש שמכבד `prefers-reduced-motion`.
- החיפוש שודרג לפאנל Spotlight ממורכז וצף מעל רקע קולנועי מטושטש.
- הוטמע סימן מותג וקטורי חדש: שלושה זרמי מדיה סגולים מתכנסים למעטפת קדמית Arctic White. הסמל שומר על צללית ברורה גם ב־16px.

### קבצים שנוצרו

- `src/renderer/src/screens/Music.tsx` — מסך Music Hub מלא: Hero, mixes, חיפוש, מדפי featured/local, Deezer previews ו־Spotify Connect.
- `src/renderer/src/assets/music-hero-v1.png` — רקע Music Hero שנוצר באמצעות ImageGen המובנה.
- `docs/brand/omniflux-mark-concept-v1.png` — קונספט המיתוג המקורי שנוצר באמצעות ImageGen ונשמר לצורכי המשך מותג.
- `tests/music.spec.ts` — שלושה תרחישי Electron: פתיחת המתחם, ניגון מוזיקה מקומית ואימות Spotlight Search.

### קבצים ששונו בסבב זה

- `src/renderer/src/store/ui.ts` — נוסף route מסוג `music`.
- `src/renderer/src/App.tsx` — רישום מסך Music, שיפור ניתוב קיצורי המקלדת והאזנה בשלב capture.
- `src/renderer/src/components/Sidebar.tsx` — יעד Music קבוע, אייקון ייעודי ו־`aria-label` מפורש לכל יעד ניווט.
- `src/renderer/src/components/ui.tsx` — נוסף glyph מוזיקה מתוך Lucide.
- `src/renderer/src/components/Mark.tsx` — החלפת סימן המותג ב־SVG חדש עם מזהה gradient ייחודי לכל מופע.
- `src/renderer/src/screens/NowPlaying.tsx` — עטיפה מוגדלת, ambient cover layer, waveform מונפש ופאנל מילים עמוק יותר.
- `src/renderer/src/screens/Search.tsx` — פריסת Spotlight מזכוכית על רקע מטושטש.
- `src/renderer/src/styles/app.css` — אנימציית waveform, מצב reduced motion, ובנוסף ליטושי הכרטיסים מהסבב הקודם.
- `src/shared/i18n/locales/fr.ts`
- `src/shared/i18n/locales/en.ts`
- `src/shared/i18n/locales/he.ts`
- `src/shared/i18n/locales/ar.ts`
- `src/shared/i18n/locales/es.ts`
- `src/shared/i18n/locales/de.ts`
- `src/shared/i18n/locales/it.ts`
- `src/shared/i18n/locales/pt.ts` — לכל שמונה השפות נוספו יעד Music ו־15 מחרוזות מלאות של המתחם; עברית וערבית נשארו RTL מלא.
- `tools/_final-shots.mjs` — צילום Music ו־Spotlight Search בצרפתית, עברית וערבית; נוסף כשל מפורש אם יעד ניווט חסר.
- `tools/_contrast.mjs` — מדידת WCAG הורחבה גם למסך Music ולחיפוש הצף.
- `package.json` — תוקן `test:unit` מ־Playwright project שאינו קיים להרצת בדיקות `src` בפועל.

הקבצים `Home.tsx`, `MiniPlayer.tsx`, `Poster.tsx`, `TitleCard.tsx`, נכסי `hero-v2.png` ו־`placeholder-v2.png`, וכן `.gitignore`, שונו או נוצרו בסבב הקודם ומתועדים בחלק הראשון של מסמך זה.

### החלטות ארכיטקטוניות ועיצוביות

- ה־Music Hub משתמש בחוזים הקיימים (`hub.data`, `search.run`, `spotify.*`, `library.catalog`) ואינו מוסיף שכבת IPC כפולה.
- כרטיסי מוזיקה הם ריבועיים כדי לשקף עטיפות אלבום; כרטיסי סרטים נשארו ביחס `2:3`.
- `activate()` נשאר שער ההפעלה היחיד: קובץ מקומי ו־Deezer preview נשלחים ל־mpv, ו־Spotify URI נשלח ל־Spotify Connect.
- ImageGen שימש למחקר מותג ולנכס Hero raster; הסמל הסופי נבנה ב־SVG/React כדי לשמור על חדות, נגישות וסקיילינג.
- צבעי האמביינט של Now Playing ממשיכים להיגזר מהעטיפה בלבד. Violet קבוע משמש רק waveform/מצב, בהתאם לשפת Obsidian.
- חיפוש נשאר route נגיש מה־Sidebar, אך מוצג חזותית כחלון Spotlight צף כדי לשמור על מודל ניווט ברור ועל תחושת שכבות.

### פרומפטים ונכסי ImageGen

- קונספט לוגו (`logo-brand`): סימן יחיד, מינימליסטי ו־vector-friendly; שלושה ribbons מתכנסים לפתח קדמי; Arctic White עם Electric Violet; ללא טקסט, אותיות, משולש Play גנרי או אפקט 3D.
- Music Hero (`stylized-concept`): רקע 16:9 של sound-wave ribbons מזכוכית Obsidian, cyan/violet מרוסנים, מרחב כהה בטוח ל־LTR/RTL, ללא טקסט, אנשים, כלי נגינה או תווי מוזיקה.

### בדיקות ואימות סופי

- `npm run typecheck` — עבר.
- `npm run test:unit` — תוקן ומריץ 155 בדיקות מקור; תכולתן עברה כחלק מסוויטת `npm test` הסופית.
- `npx playwright test tests/music.spec.ts` — **3 passed**.
- `npm test` — **217 passed**, **2 skipped**, 0 failed. שתי הבדיקות המדולגות תלויות בתוכן רשת של Deezer.
- `npm run verify:contrast` — **89 רכיבים**, 0 מתחת ל־WCAG AA; יחס מינימלי `5.23:1`.
- `npm run verify:shots` — עבר; הופקו 15 צילומים: Home, Music, Library, Settings ו־Search בצרפתית, עברית וערבית.
- `npm run build` — עבר; main, preload ו־renderer נבנו עם נכס Music Hero החדש.
- `git diff --check` — נקי; אזהרות LF/CRLF בלבד לפי תצורת Git המקומית.

### קבצים שנמחקו

- לא נמחקו קבצים.

---

## 2026-08-23 — Video Player Pro, PiP וניהול מנויים חכם

### תוצאה מוצרית

- פאנל התמונה של הנגן הפך מכלי בסיסי למערכת עיבוד חיה של `mpv`: בהירות, ניגודיות, רוויה, גמא, Sharpen, Deband, יחס תצוגה ומצבי Fit / Fill / Zoom.
- סנכרון האודיו והכתוביות מוצג ונשלט במילישניות, תוך שמירת חוזה המנוע הפנימי בשניות כפי ש־mpv מצפה.
- נוספו מסך מלא ברמת חלון המערכת ו־Picture-in-Picture צף, כולל החזרת החלון המדויקת לגודל ולמיקום הקודמים.
- פקדי ה־PiP מותאמים לחלון הקומפקטי: Play/Pause, דילוג 10 שניות ויציאה מ־PiP, בלי להעמיס את פאנל הכלים המלא.
- באנר ה־Hero וכרטיסי הקטלוג מזהים שירות שנמצא ברשימת המנויים, מציגים תג Mint של "כלול במנוי שלך" ופותחים קישור שירות ישיר כאשר קיימת התאמה.
- שינוי מנוי מבטל את מטמון ה־Hub ומרענן אותו ברקע, כך שהתגיות וקריאות הפעולה אינן נשארות במצב מיושן.

### החלטות ארכיטקטוניות

- ארבע התאמות הצבע משתמשות ב־properties המקוריים של mpv ונצפות כחלק מ־`EngineState`; המנוע נשאר מקור האמת היחיד.
- Sharpen מיושם כפילטר Lavfi מסומן בשם `@omni-sharpen`, ולכן ניתן להחליף או להסיר רק אותו בלי לפגוע בפילטרים אחרים. ערך אפס מסיר את הפילטר.
- Deband משתמש באפשרות הווידאו המקורית של mpv. Fit / Fill / Zoom ממופים ל־`panscan` ול־`video-zoom` באופן דטרמיניסטי.
- בחלון השקוף וחסר המסגרת של Windows, דגלי fullscreen של Electron אינם מיושמים באופן אמין. לכן Windows משתמש ב־borderless fullscreen בגבולות הפיזיים של התצוגה ושומר Rectangle לשחזור; פלטפורמות אחרות משתמשות ב־`setFullScreen`.
- PiP אינו חלון וידאו מדומה: חלון Electron וחלון mpv המוצמד אליו משנים יחד גבולות, נשארים מעל חלונות אחרים ומוחזרים יחד.
- חוזה ה־IPC הורחב דרך `src/shared/api.ts` ו־preload בלבד; ה־renderer אינו מקבל גישת Node או Electron ישירה.
- נכסי ImageGen הקיימים נבדקו חזותית ונשמרו ללא יצירת כפילויות: קונספט המותג מתאים להמשך מחקר, וה־Music Hero מספק מרחב כהה תקין ל־LTR ול־RTL. הסמל הפעיל נשאר SVG מקומי וחד.

### קבצים ששונו בסבב זה

- `src/main/engine/mpv.ts` — מצב מנוע מורחב, properties של תמונה, Sharpen, Deband, מצבי התאמה ואיפוס מלא.
- `src/main/window/player.ts` — fullscreen חוצה־פלטפורמות, borderless fullscreen ל־Windows, PiP ושחזור Rectangle.
- `src/main/index.ts` — handlers חדשים ל־IPC ורענון Hub לאחר שינוי מנוי.
- `src/main/services/hub.ts` — זיהוי מנוי בכרטיסי Trending וב־Hero וקישור ישיר לשירות.
- `src/preload/index.ts` ו־`src/shared/api.ts` — חוזי window/player חדשים ושדות מצב וידאו.
- `src/renderer/src/App.tsx` — תזמור fullscreen/PiP, קיצור `F`, יציאה ב־Escape ובקרות PiP קומפקטיות.
- `src/renderer/src/components/Controls.tsx` — פעולות fullscreen ו־PiP בשכבת הבקרות הקולנועית.
- `src/renderer/src/components/PowerPanel.tsx` — פאנל תמונה מלא וסנכרון במילישניות.
- `src/renderer/src/components/MiniPlayer.tsx` — כניסה למסך מלא אמיתי עבור וידאו.
- `src/renderer/src/components/TitleCard.tsx` — תג Mint וקריאת פעולה לכותר הכלול במנוי.
- `src/renderer/src/components/ui.tsx` — אייקון Picture-in-Picture מתוך Lucide.
- `src/renderer/src/screens/Home.tsx` — מצב "כלול במנוי שלך" ב־Hero.
- `src/renderer/src/store/player.ts` — ערכי ברירת מחדל למצב עיבוד התמונה.
- כל שמונת קובצי `src/shared/i18n/locales/*.ts` — תרגומי PiP, בקרות תמונה, מצבי התאמה ומילישניות.
- `tests/power.spec.ts` — בדיקות אינטגרציה לפילטרים, איפוס, PiP, שחזור חלון ו־fullscreen.
- `CHANGELOG_DEV.md` — סעיף מסירה זה.

### קבצים שנוצרו או נמחקו בסבב זה

- לא נוצרו ולא נמחקו קבצים בסבב זה. נעשה שימוש חוזר בנכסי המותג וה־Music Hero שנוצרו ותועדו בסבב הקודם.

### בדיקות ואימות

- `npm run typecheck` — עבר ללא שגיאות.
- `npm run build` — עבר; main, preload ו־renderer נבנו עם חוזי ה־IPC החדשים.
- `npx playwright test tests/power.spec.ts` — **8 passed**; כולל פילטר Sharpen אמיתי מול mpv, Deband, איפוס, PiP ו־fullscreen.
- `npm test` — **220 passed**, **2 skipped**, 0 failed. שתי הבדיקות המדולגות תלויות בתוכן רשת חיצוני של Deezer.
- `npm run verify:contrast` — **138 אלמנטים**, 0 מתחת ל־WCAG AA; יחס מינימלי `5.12:1`.
- `npm run verify:shots` — עבר; 15 צילומי Home, Music, Library, Settings ו־Search בצרפתית, עברית וערבית.
- `git diff --check` — נקי מלבד אזהרות LF/CRLF של סביבת Windows.

---

## 2026-08-23 — חיבור כתוביות מקוונות באמצעות SubDL

### תוצאה מוצרית

- נוסף חיפוש כתוביות מקוון מתוך שכבת הבקרות הקולנועית של הנגן. פתיחת כפתור הכתוביות המקוונות מחפשת אוטומטית לפי שם קובץ המדיה המדויק, בשפת הממשק הפעילה ובאנגלית.
- כל תוצאה מציגה release, שפה, ציון התאמה, FPS וסימון ללקויי שמיעה. לחיצה מורידה את הארכיון, מחלצת קובץ כתוביות נתמך וטוענת אותו ישירות ל־`mpv`.
- החיפוש נשמר במטמון זיכרון לעשר דקות והקובץ המחולץ נשמר תחת `userData/subtitles`; חיפוש או טעינה חוזרים אינם צורכים בקשת רשת נוספת כל עוד המטמון תקף.
- נוספו תרגומים מלאים לממשק הכתוביות המקוונות בכל שמונה השפות, כולל יישור RTL בעברית ובערבית.

### אבטחה והחלטות ארכיטקטוניות

- `SUBDL_API_KEY` נקרא רק בתהליך הראשי: תחילה ממשתנה התהליך, ובפיתוח בלבד מתוך `.env`. המפתח אינו נכנס ל־bundle של ה־renderer, אינו עובר ב־IPC ואינו נכתב ללוגים. `.env` כבר מוחרג ב־`.gitignore` ולא שונה בסבב זה.
- החיפוש משתמש ב־SubDL API v2 ובכותרת `Authorization: Bearer`, כך שהמפתח אינו מופיע בכתובת החיפוש.
- כתובת ההורדה החתומה נשמרת במפה פרטית בתהליך הראשי. ה־renderer מקבל מזהה SHA-256 אטום באורך 24 תווים בלבד.
- קישורי הורדה מתקבלים רק כנתיב יחסי תחת `https://dl.subdl.com/subtitle/`, עם ZIP ופרמטר `api_key` החתום של הספק בלבד. כתובות חיצוניות, query לא מוכר, backslash ו־path traversal נדחים.
- חילוץ ZIP מבוצע בתוך Node ללא shell וללא תלות חדשה. נתמכים רק SRT/ASS/SSA/VTT/SUB, קבצים מוצפנים ושיטות דחיסה לא מוכרות נדחים, וגודל הארכיון/הפלט מוגבל ל־25MB/10MB כדי למנוע zip bomb.
- ה־IPC אינו מקבל URL מה־renderer. פעולת ההורדה מקבלת רק מזהה שנוצר בחיפוש האחרון, ולכן אינה יכולה לשמש כ־SSRF או כקורא קבצים שרירותי.

### קבצים שנוצרו

- `src/adapters/provider-subdl/index.ts` — מתאם SubDL v2, נרמול תוצאות וחילוץ ZIP מוגבל ובטוח.
- `src/adapters/provider-subdl/__tests__/subdl.spec.ts` — בדיקות אימות בכותרת, נרמול, חסימת URL מסוכן וחילוץ ZIP.
- `src/main/services/online-subtitles.ts` — קריאת תצורה מאובטחת, מטמון חיפוש, מזהים אטומים, מטמון הורדות ושמירה תחת `userData`.

### קבצים ששונו

- `src/main/index.ts` — רישום IPC לסטטוס הספק, חיפוש, הורדה וטעינה ל־mpv.
- `src/preload/index.ts` ו־`src/shared/api.ts` — חוזה IPC מוקלד לכתוביות מקוונות וסוג `OnlineSubtitle` בטוח ל־renderer.
- `src/renderer/src/components/Controls.tsx` — פאנל SubDL צף עם מצבי טעינה, תוצאות, שגיאות וטעינה בלחיצה.
- כל שמונת קובצי `src/shared/i18n/locales/*.ts` — שמונה מחרוזות חדשות לכל שפה.
- `CHANGELOG_DEV.md` — סעיף מסירה זה.

### קבצים שנמחקו

- לא נמחקו קבצים.

### בדיקות ואימות

- חיבור SubDL החי אומת בלי להציג את המפתח: חיפוש v2 החזיר תוצאה תקינה, וקישור ההורדה החתום החזיר ZIP בגודל 51,765 בתים ובו קובץ כתוביות נתמך אחד. תוכן הבדיקה לא נשמר בדיסק.
- `npm run typecheck` — עבר ללא שגיאות.
- `npx playwright test src/adapters/provider-subdl src/shared/i18n` — **42 passed**.
- `npm test` — **224 passed**, **2 skipped**, 0 failed. שתי הבדיקות המדולגות תלויות בתוכן רשת חיצוני של Deezer.
- `npm run build` — עבר; main, preload ו־renderer נבנו בהצלחה.

---

## 2026-08-23 — Critical Architecture & Feature Overhaul

### תוצאה מוצרית

- תוצאות Spotify מקבלות כתובת Deezer Preview תואמת לפי שם שיר ואמן. כאשר Spotify Connect אינו מחובר או שפקודת הניגון נכשלת, הנגן עובר אוטומטית ל־Preview בתוך OmniFlux במקום להציג שגיאת "אין מכשיר פעיל" בלבד.
- מתחם המוזיקה הייעודי חובר ל־Deezer Charts ומציג מדפים נפרדים של רצועות מובילות, אמנים מומלצים ואלבומים פופולריים, לצד מוזיקה מקומית, חיפוש ו־Spotify Connect.
- נוספו תגי מותג וקטוריים קומפקטיים של Spotify ו־Deezer לכרטיסי מוזיקה, למסך המוזיקה ול־MiniPlayer.
- החיפוש המאוחד שופר: קבצים מקומיים, Drive, סרטים/סדרות מ־TMDB, Spotify ו־Deezer ממשיכים להישאל במקביל; התאמה מקומית סובלת שגיאת כתיב קטנה, ותקלת/איטיות watch-provider בודדת אינה מוחקת תוצאת סרט או סדרה תקינה. תרחיש "The Mentalist" אומת כתוצאת סטרימינג לפני מוזיקה.
- מסך הבית מחובר למדפים חיים של TMDB: Top 10 שבועי, סרטים בדירוג הגבוה ביותר וסדרות פופולריות.
- חלון OmniFlux הראשי מקבל אייקון אפליקציה מפורש ו־Windows App User Model ID קבוע. חלון הווידאו הנפרד של mpv מוגדר ללא כפתור Taskbar וללא taskbar progress, כדי למנוע את אייקון ה־Electron/mpv הכפול.
- כרטיס פרטי כותר מציג CTA נפרד להרשמה כאשר השירות אינו מסומן כמנוי, באמצעות דפי ההרשמה הרשמיים של Netflix, Prime Video, Apple TV+, Disney+, Max, Paramount+, Canal+ ו־Crunchyroll.
- תוקן מעבר PiP ב־Windows: החלון עובר קודם בגודל ובמיקום native מפורשים, ורק לאחר מכן ננעל לשינוי גודל. כך הוא אינו נשאר בטעות בגודל 1440px ומוחזר במדויק לגבולות הקודמים.

### החלטות ארכיטקטוניות ומגבלות ספקים

- החלטה זו הוחלפה בעדכון "Critical Playback Routing, Trailers & Embedded Spotify" שלהלן: נוסף משטח Spotify Web Playback SDK מבודד ונסתר, עם fallback שקוף ל־Deezer כאשר Premium או EME אינם זמינים.
- TMDB Watch Providers אינו מספק מזהי asset מלאים של Netflix/Prime/Disney או deep-link ישיר לכל כותר. לכן OmniFlux אינו ממציא מזהים: כשאין URL ישיר מאומת הוא משתמש בחיפוש ממוקד בשירות וב־CTA הרשמה רשמי. מעבר ל־deep link מדויק דורש API/מזהה תוכן מורשה מכל ספק.
- התחברות לשירותים מוגנים נשארת בדפדפן המערכת ולא ב־`webview` מוטמע. כך נשמרים גבולות `contextIsolation`, נמנעת חשיפת cookies לתהליך ה־renderer ונמנעת הבטחה שגויה להפעלת DRM בתוך Chromium של Electron.
- חוזי המוזיקה והמדפים הורחבו רק דרך `src/shared/api.ts` וה־preload המבודד; ה־renderer אינו מקבל גישת Node או סודות API.

### קבצים שנוצרו

- `src/renderer/src/components/MusicProviderBadge.tsx` — תגי Spotify/Deezer וקטוריים ונגישים.
- `src/adapters/provider-deezer/__tests__/deezer.spec.ts` — בדיקת נרמול Deezer Charts.
- `src/core/search/__tests__/search.spec.ts` — בדיקות fallback של מוזיקה וחיפוש fuzzy.
- `src/main/__tests__/windows-shell.spec.ts` — אימות סטטי של אייקון, App ID והסתרת חלון mpv משורת המשימות.

### קבצים ששונו

- `electron-builder.yml` — אריזת `build/icon.png` כ־`app-icon.png` במשאבי היישום.
- `src/adapters/provider-deezer/index.ts` — Deezer Charts ומיפוי tracks/artists/albums.
- `src/adapters/provider-tmdb/index.ts` — `movie/top_rated` ו־`tv/popular`.
- `src/core/search/index.ts`, `src/core/search/types.ts` — fuzzy matching, timeouts מבודדים ו־Deezer fallback לתוצאת Spotify.
- `src/core/library/types.ts`, `src/main/services/hub.ts` — סוגי שורות ויצירת מדפי TMDB החיים, signup URLs ו־Top 10.
- `src/main/index.ts`, `src/main/window/player.ts`, `src/main/engine/mpv.ts` — App User Model ID, אייקון חלון, IPC discovery, הסתרת חלון המנוע ותיקון PiP.
- `src/preload/index.ts`, `src/shared/api.ts` — חוזי IPC ל־Music Discovery ושדות fallback/signup.
- `src/shared/deeplink.ts`, `src/shared/__tests__/deeplink.spec.ts` — דפי הרשמה רשמיים ובדיקות URL.
- `src/renderer/src/lib/activate.ts` — מעבר אוטומטי לניגון Deezer כאשר Spotify אינו זמין.
- `src/renderer/src/screens/Music.tsx`, `src/renderer/src/screens/Home.tsx`, `src/renderer/src/screens/TitleDetails.tsx` — מדפי מוזיקה, מדפי TMDB ו־CTA הרשמה.
- `src/renderer/src/components/TitleCard.tsx`, `src/renderer/src/components/MiniPlayer.tsx` — תגי ספק מוזיקה.
- כל שמונת קובצי `src/shared/i18n/locales/*.ts` — שמות המדפים ופעולת ההרשמה בצרפתית, אנגלית, עברית, ערבית, ספרדית, גרמנית, איטלקית ופורטוגזית.
- `tests/music.spec.ts`, `tests/search.spec.ts`, `tests/power.spec.ts` — תרחישי Music Hub, קדימות סרט/סדרה ו־PiP אמיתי.
- `CHANGELOG_DEV.md` — סעיף מסירה זה.

### קבצים שנמחקו

- לא נמחקו קבצים.

### בדיקות ואימות סופי

- `npm run typecheck` — עבר ללא שגיאות.
- `npm run build` — עבר; main, preload ו־renderer נבנו בהצלחה.
- בדיקות ממוקדות למוזיקה, חיפוש ו־Windows shell — **15 passed**, **1 skipped** (בדיקת Deezer חיה התלויה ברשת).
- בדיקת PiP חזרה שלוש פעמים ברצף — **3 passed**.
- `npm test` — **231 passed**, **2 skipped**, 0 failed מתוך 233. הדילוגים הם תרחישי Deezer חיים התלויים בזמינות רשת/תוכן חיצוני.
- `npm run verify:contrast` — **146 אלמנטים**, 0 מתחת ל־WCAG AA; היחס הנמוך ביותר `5.12:1`.
- `npm run verify:shots` — עבר; הופקו צילומי Home, Music, Library, Settings ו־Search בצרפתית LTR, עברית RTL וערבית RTL.
- `git diff --check` — נקי; הופיעו רק אזהרות LF/CRLF צפויות של סביבת Windows.

---

## 2026-08-23 — Critical Playback Routing, Trailers & Embedded Spotify

### תיקוני התנהגות ופונקציונליות

- הופרדו לחלוטין מסלולי הפעולה של וידאו ומוזיקה. תוצאת TMDB מסוג סרט או סדרה פותחת תמיד את חלון פרטי הכותר; נוסף guard בשכבת ההפעלה המשותפת שמונע מתוצאת `catalog` להגיע ל־Spotify, Deezer או mpv גם אם רכיב UI יקרא לה בטעות.
- החיפוש המאוחד ממשיך להריץ במקביל ספרייה מקומית, Google Drive, TMDB לסרטים ולסדרות וספקי מוזיקה, ומציג קבוצות נפרדות. נוספה בדיקת רגרסיה חיה עבור “The Batman” שמוודאת פתיחת פרטים והיעדר שגיאת Spotify.
- מנוע Deezer מעביר כעת אל mpv אובייקט ניגון מוקלד הכולל שם רצועה, אמן, מזהה ספק, עטיפת `cover_xl` ומצב `preview`/`full`. שם CDN או hash אינו יכול עוד לדרוס את שם הרצועה ב־MiniPlayer.
- כרטיס המוזיקה הפעיל מקבל סימון סגול ואיקולייזר מונפש; ה־MiniPlayer מציג עטיפה באיכות גבוהה, אמן, תג Deezer וחיווי מפורש של Preview באורך 30 שניות.
- TMDB Videos חובר עבור סרטים וסדרות. נבחר קודם Trailer רשמי ב־YouTube, לאחר מכן Trailer/Teaser מתאים, עם fallback לשפה האנגלית. כפתור `Bande-annonce` נוסף גם ל־Hero וגם לחלון הפרטים, והווידאו נפתח בנגן responsive של `youtube-nocookie.com` בתוך האפליקציה.
- נוסף Spotify Web Playback SDK במשטח `BrowserWindow` נסתר ומבודד, עם `skipTaskbar`, `sandbox`, ללא Node integration וללא גשר preload של OmniFlux. המכשיר הפנימי מופעל לפני Spotify Connect ומקבל אסימון זמני בלבד. כשל אתחול מאפס את המשטח ומאפשר ניסיון חוזר ללא הפעלה מחדש.
- הרשאות OAuth של Spotify הורחבו למינימום הנדרש לנגן Web Playback. אסימונים ישנים מסומנים לחיבור מחדש פעם אחת כדי לקבל את הרשאת `streaming` והיקפי החשבון הנדרשים.
- אם Spotify SDK אינו נתמך, ההפעלה נופלת ל־Deezer Preview תואם בתוך OmniFlux; אם אין Preview, נפתח קישור הרצועה המדויק ב־Spotify במקום להציג את שגיאת “Aucun appareil Spotify actif”.
- חלון Spotify הנסתר אינו יוצר אייקון נוסף בשורת המשימות, נסגר בניתוק/יציאה, וסגירת החלון הראשי מסיימת את האפליקציה כך שלא נותר תהליך רקע סמוי.
- קישורי ספקים נשארו קישורי חיפוש ממוקדים לשם הכותר בתוך Netflix, Prime Video, Max ושירותים נתמכים, לצד דפי הרשמה רשמיים למי שאינו מנוי. TMDB Watch Providers אינו מחזיר מזהי asset של הספקים או deep links מלאים, ולכן לא הומצאו מזהים לא מאומתים. מעבר לכתובת asset מדויקת מחייב מזהה מורשה מהספק הרלוונטי.

### החלטות ארכיטקטוניות ואבטחה

- נגן Spotify לא נטען בתוך renderer הראשי: נוצר entry עצמאי ב־Vite וקובץ HTML עם CSP מצומצם. כך ספריית צד שלישי מרוחקת אינה מקבלת גישה ל־`window.cinema` או ל־IPC של האפליקציה.
- מטא־דאטה של רצועה הוא חלק מחוזה `PlaybackRequest` המשותף ואינו מוסק עוד מכתובת הזרם. עבור קבצים מקומיים נשמר מנגנון פענוח שם הקובץ הקיים.
- נגן הטריילר משתמש ב־portal ברמת `document.body`, מאמת את מפתח YouTube ומגביל את ה־CSP ל־YouTube Privacy-Enhanced בלבד.
- Spotify Web Playback נשאר capability-dependent: חשבון Premium וסביבת EME נתמכת הם תנאי ספק. אין הבטחה להפעלת DRM בכל הפצת Electron, ולכן מסלול Deezer נשמר כחלק מהארכיטקטורה ולא רק כטיפול בשגיאה.

### קבצים שנוצרו

- `src/main/services/spotify-web-playback.ts` — ניהול חלון Spotify נסתר, lifecycle, device ID והתאוששות מכשל.
- `src/renderer/spotify-player.html` — מסמך מבודד עם CSP ייעודי ל־Spotify.
- `src/renderer/src/spotify-player.ts` — אתחול Web Playback SDK וחיבור מכשיר OmniFlux.
- `src/renderer/src/components/TrailerPlayer.tsx` — נגן טריילר מוטמע, responsive ונגיש.
- `src/adapters/provider-tmdb/__tests__/tmdb.spec.ts` — בדיקת בחירת טריילר רשמי ו־fallback לשפה האנגלית.

### קבצים ששונו

- `electron.vite.config.ts`, `src/renderer/index.html` — entry עצמאי לנגן Spotify והרשאת CSP ל־YouTube Privacy-Enhanced.
- `src/shared/api.ts`, `src/main/engine/mpv.ts`, `src/main/index.ts` — חוזה Playback עשיר, שמירת מטא־דאטה ולifecycle של Spotify.
- `src/main/services/spotify-auth.ts`, `src/main/services/spotify-connect.ts` — scopes ל־Web Playback והעדפה ישירה של המכשיר המוטמע.
- `src/adapters/provider-deezer/index.ts` — שימוש בעטיפות Deezer ברזולוציה הגבוהה ביותר הזמינה.
- `src/adapters/provider-tmdb/index.ts`, `src/main/services/hub.ts` — איתור Trailer והוספתו ל־Hero ולפרטי כותר.
- `src/renderer/src/lib/activate.ts`, `src/renderer/src/screens/Search.tsx` — בידוד קשיח בין סרטים למוזיקה ופתיחת Movie Details מתוצאות קטלוג.
- `src/renderer/src/App.tsx`, `src/renderer/src/components/MiniPlayer.tsx`, `src/renderer/src/screens/Music.tsx`, `src/renderer/src/screens/NowPlaying.tsx`, `src/renderer/src/store/player.ts` — מטא־דאטה, עטיפה, מצב רצועה וחיווי Preview.
- `src/renderer/src/screens/Home.tsx`, `src/renderer/src/screens/TitleDetails.tsx` — כפתורי Trailer והנגן המוטמע.
- כל שמונת קובצי `src/shared/i18n/locales/*.ts` — תרגום פעולת הטריילר בצרפתית, אנגלית, עברית, ערבית, ספרדית, גרמנית, איטלקית ופורטוגזית.
- `src/main/__tests__/spotify-auth.spec.ts`, `src/main/__tests__/windows-shell.spec.ts`, `tests/hub.spec.ts`, `tests/music.spec.ts`, `tests/search.spec.ts` — בדיקות scopes, בידוד חלון, Trailer, מטא־דאטה ורגרסיית ניתוב סרטים.

### קבצים שנמחקו

- לא נמחקו קבצים.

### בדיקות ואימות סופי

- `npm test` — **234 passed**, **2 skipped**, 0 failed מתוך 236. שני הדילוגים הם תרחישי Deezer חיים התלויים בזמינות רשת/תוכן חיצוני.
- `npm run typecheck` — עבר ללא שגיאות TypeScript.
- `npm run build` — עבר; main, preload ושני יעדי renderer נבנו בהצלחה, כולל `spotify-player.html` וה־bundle המבודד שלו.
- `npm run verify:contrast` — **147 אלמנטים**, 0 מתחת ל־WCAG AA; היחס הנמוך ביותר `5.12:1`.
- `npm run verify:shots` — עבר; הופקו צילומי Home, Music, Library, Settings ו־Search בצרפתית LTR, עברית RTL וערבית RTL.
- `git diff --check` — נקי; הופיעו רק אזהרות LF/CRLF צפויות של סביבת Windows.
- לא בוצעה בדיקת Spotify Premium חיה משום שהיא דורשת אישור OAuth אינטראקטיבי, חשבון Premium ותמיכת EME במכונת היעד. ה־scopes, הבידוד, lifecycle וה־build אומתו אוטומטית.

---

## 2026-08-24 — Full-Length Spotify & Music Feature Benchmark

### ניגון מלא ו־DRM

- הוחלף runtime הפיתוח של Electron בהפצת CastLabs ECS ‏`43.0.0+wvcus`, התואמת ל־Electron 43 ומוסיפה Component Updater רשמי ל־Google Widevine. מקור החבילה נעול ל־HTTPS ב־`package.json` וב־`package-lock.json`.
- `components.whenReady()` מופעל באופן עצל רק כאשר נדרש משטח Spotify. מעטפת OmniFlux אינה ממתינה להורדת Widevine; נגן Spotify ממתין להתקנה/עדכון לפני יצירת מכשיר ה־SDK.
- משטח Spotify הנסתר נשאר `sandbox: true`, ללא Node integration, עם `skipTaskbar: true`, ‏`plugins: true` ו־`backgroundThrottling: false`.
- Web Playback SDK מקבל Spotify URI ולא `preview_url`. מצב הנגן חוזר בכל שנייה ל־renderer וכולל כותר, אמן, אלבום, עטיפה, משך מלא, position, volume, shuffle ו־repeat.
- נוסף רענון token ברקע כל 30 דקות. callback של `getOAuthToken` משתמש תמיד באסימון האחרון, ולכן רצועה או queue ארוכים אינם נעצרים עם פקיעת access token ראשון.
- מעבר Spotify → קובץ מקומי/Deezer משהה את Spotify ומחזיר את מקור האמת ל־mpv. מעבר בכיוון ההפוך משהה mpv ומעביר את ה־MiniPlayer ואת Now Playing למצב Spotify. כך נמנע ניגון כפול.

### פקדי נגן, queue ומילים

- נוספו Play/Pause, Previous, Next, seek במילישניות, volume, Shuffle ו־Repeat במצבי Off / All / One דרך Spotify Web API.
- הפקדים עובדים גם ב־MiniPlayer וגם במסך Now Playing; פקדי וידאו/כתוביות/Power Panel מוסתרים כאשר Spotify הוא הספק הפעיל.
- לחיצה על רצועה בתוך רשימה מוסיפה לתור, לפי הסדר, עד עשר מהרצועות הבאות. נוסף drawer להצגת ה־queue ופעולת Add to Queue לכל רצועה.
- מצב Spotify ממופה ל־EngineState התצוגתי עם `playbackMode: full`; פס ההתקדמות והזמנים מציגים את משך השיר המלא ולא 30 שניות.
- LRCLIB מקבל את שם הרצועה, האמן והמשך המלא מ־Spotify. הסנכרון, ההדגשה וה־auto-scroll הקיימים פועלים גם בניגון Spotify.
- equalizer מונפש וסימון הרצועה הפעילה נשמרו בכל רשימות Spotify.

### Music Hub אישי

- נוספו מדפי Spotify מאומתים: Top Items לטווח הקצר (“Made for you”), ‏Liked Songs, פלייליסטים פרטיים/שיתופיים, mix playlists ו־followed artists.
- נוספה יצירת פלייליסט פרטי דרך endpoint ‏`POST /me/playlists` העדכני.
- Liked Songs משתמש ב־API המאוחד של 2026: ‏`PUT/DELETE /me/library` עם Spotify URI, במקום endpoints הישנים של tracks/albums.
- נוסף מסך אמן פנימי עם חמש רצועות מחיפוש exact-artist ודיסקוגרפיית Albums & Singles.
- נוסף מסך אלבום עם עטיפה, אמן, שנת יציאה, tracklist מלא, משך לכל רצועה וסך זמן האלבום.
- כל מטא־דאטה ועטיפה של Spotify מוצגים עם תג Spotify וקישור השירות הקיים, בהתאם לכללי attribution.
- הרחבות הממשק תורגמו לצרפתית, אנגלית, עברית, ערבית, ספרדית, גרמנית, איטלקית ופורטוגזית ונבדקו ב־LTR/RTL.

### Benchmark ומגבלות Spotify 2026

- נוצר `docs/SPOTIFY_BENCHMARK.md` עם מיפוי capability-by-capability, ארכיטקטורת DRM ודרישות runtime.
- Spotify הסירה בפברואר 2026 את Artist Top Tracks ואת Related Artists מה־endpoint set הנתמך. OmniFlux אינו ממציא popularity או related artists: חמש רצועות האמן מגיעות מחיפוש exact-artist ומסומנות “Artist tracks”.
- Recommendations/Browse charts אינם חלק מה־endpoint set הנתמך לאחר המיגרציה. המלצות Spotify מבוססות על Top Items ועל הפלייליסטים האישיים של המשתמש; global charts ממשיכים להגיע מ־Deezer ומסומנים בהתאם. לא מוצג “Spotify regional chart” מזויף.
- Spotify Player Queue מאפשר קריאה והוספה, אך אינו מספק endpoint להסרת פריט בודד או לסידור מחדש של תור הנגן. OmniFlux אינו מציג פעולה שאינה ניתנת לביצוע.

### הרשאות OAuth

- `SCOPE_VERSION` הועלה ל־3. משתמשים שכבר התחברו נדרשים לאשר מחדש פעם אחת.
- נוספו רק ה־scopes הדרושים ל־Top Items, ספרייה, followed artists, פלייליסטים, Liked Songs וניהול ניגון. לא נוספה הרשאת recently-played או גישה מיותרת אחרת.

### קבצים שנוצרו

- `src/main/services/spotify-music.ts` — Spotify Music API אישי, מיפוי metadata, אמן/אלבום, ספרייה ופלייליסטים.
- `src/main/__tests__/spotify-music.spec.ts` — בדיקות endpoints של 2026, metadata מלא ופקדי Spotify.
- `docs/SPOTIFY_BENCHMARK.md` — דו״ח השוואת יכולות ומגבלות ספק.

### קבצים ששונו

- `package.json`, `package-lock.json` — מעבר ל־CastLabs ECS/Widevine דרך מקור HTTPS נעול.
- `src/main/index.ts`, `src/main/services/spotify-web-playback.ts`, `src/main/services/spotify-connect.ts`, `src/main/services/spotify-auth.ts` — Widevine lifecycle, OAuth v3, מצב נגן, queue, transport וספריית Spotify.
- `src/shared/api.ts`, `src/preload/index.ts` — חוזי IPC מוקלדים ל־Spotify Hub, playback state, artists, albums, playlists, queue ו־lyrics lookup מפורש.
- `src/renderer/src/spotify-player.ts` — token rotation ו־Media Element activation ב־Web Playback SDK.
- `src/renderer/src/store/player.ts`, `src/renderer/src/lib/activate.ts`, `src/renderer/src/App.tsx` — arbitration בין mpv ל־Spotify ומיפוי מצב מלא ל־UI.
- `src/renderer/src/components/MiniPlayer.tsx`, `src/renderer/src/components/Controls.tsx`, `src/renderer/src/components/ui.tsx` — פקדי Spotify מלאים, seek, shuffle/repeat ואייקוני queue/like.
- `src/renderer/src/screens/Music.tsx`, `src/renderer/src/screens/NowPlaying.tsx` — Music Hub אישי, modals של אמן/אלבום, queue, playlists ומילים מסונכרנות.
- כל שמונת קובצי `src/shared/i18n/locales/*.ts` — מחרוזות חדשות לכל יכולות המוזיקה ב־LTR וב־RTL.
- `src/main/__tests__/spotify-auth.spec.ts`, `src/main/__tests__/windows-shell.spec.ts` — בדיקות scopes, CastLabs/Widevine ובידוד חלון הרקע.

### קבצים שנמחקו

- לא נמחקו קבצים.

### בדיקות ואימות

- `npm test` — **237 passed**, **2 skipped**, 0 failed מתוך 239. שני הדילוגים הם תרחישי Deezer חיים התלויים בזמינות רשת/תוכן חיצוני.
- `npm run typecheck` — עבר ללא שגיאות TypeScript.
- `npm run build` — עבר; main, preload, renderer הראשי ו־Spotify renderer המבודד נבנו בהצלחה.
- `npm run verify:contrast` — **140 אלמנטים**, 0 מתחת ל־WCAG AA; היחס הנמוך ביותר `5.12:1`.
- `npm run verify:shots` — עבר בצרפתית LTR, עברית RTL וערבית RTL עבור Home, Music, Library, Settings ו־Search.
- `npm install` / audit — 0 vulnerabilities.
- `git diff --check` — נקי; אזהרות LF/CRLF בלבד בסביבת Windows.
- לא בוצעה הפעלת שיר Spotify Premium חי מקצה לקצה, משום שהבדיקה דורשת חשבון Premium והרשאת OAuth אינטראקטיבית שאינם חלק מסביבת הבדיקות. מבנה Widevine, ה־SDK, ה־scopes, ה־IPC, ה־state mapping וה־fallback אומתו בבנייה ובבדיקות אוטומטיות.

---

## 2026-08-24 — Playback, Lyrics, Drive Metadata & Legal Compliance Sprint

### סנכרון מילים וחוויית Now Playing

- אזור המילים ב־`NowPlaying.tsx` מורכז אופקית ואנכית בתוך שכבת הזכוכית, עם רוחב קריא וקבוע גם בפריסות RTL.
- שורת LRC הפעילה מחושבת ישירות מזמן הניגון הנוכחי, מוצגת בלבן מלא (`#FFFFFF`) ובמשקל מוגדל. שורות לא פעילות מוצגות ב־`#6E7480`, מעומעמות ומטושטשות קלות.
- כל מעבר שורה מפעיל `scrollIntoView({ behavior: 'smooth', block: 'center' })`, כולל מרווחי קצה שמאפשרים למרכז גם את השורה הראשונה והאחרונה.

### Play / Pause אופטימי וניגון מוזיקה

- נוספה ל־player store פעולת `togglePlayback` מאוחדת עם מצב אופטימי: האייקון והמצב החזותי מתחלפים מיד, בעוד פקודת mpv או Spotify נשלחת ברקע.
- נוסף מונה גרסה לפקודות transport כדי שמענה ישן לא ידרוס לחיצות Play/Pause מהירות; MiniPlayer, Controls, קיצור הרווח והפקדים הצפים משתמשים כולם באותו נתיב.
- ניגון Spotify Premium ממשיך דרך Web Playback SDK המאובטח והפנימי. כשל Spotify מחפש כעת התאמת Deezer דרך IPC ומנגן את ה־preview בתוך mpv, ללא פתיחת דפדפן חיצוני.
- נוספו כפתורי הרשמה רשמיים ל־Spotify Premium ול־Deezer, עם נוסחים מלאים בכל שמונה השפות והבחנה ברורה בין שיר מלא לבין תצוגת Deezer.

### Google Drive ומטא־דאטה

- מנקה שמות הקבצים מסיר תגיות release וטכניות דוגמת `1080p`, `WEBRip`, `x264`, מפרידי נקודה/קו תחתון וכן את חתימת המעלה `סרטים השחר`.
- כרטיס Drive שאינו אודיו פותח כעת Movie Details במקום להישאר ללא תגובה. שירות ה־Hub פותר מזהה `library:*`, מעשיר התאמת TMDB קיימת בפרטים מלאים, צוות שחקנים, ספקים וטריילר, ומשמר את מזהה קובץ Drive לצורך ניגון.
- מטא־דאטה של TMDB בן יותר מ־180 יום מתרענן בסריקה הבאה, במקום להישמר ללא מגבלת זמן.

### מיתוג, Legal וייחוסים

- נוצר SVG חד ושקוף של `Built by Alexandre Heymann`, המבוסס על הגרפיקה הרשמית שסופקה, והוטמע ב־Settings / About ובפינת ה־Sidebar.
- נוסף סמל TMDB המאושר וייחוס TMDB הנדרש במסך About.
- נוספו חלונות נגישים ומקומיים ל־Conditions d'utilisation ול־Politique de confidentialité, עם תנאי שימוש אישי, מדיניות cache, נתונים מקומיים, OAuth, בקשות לשירותים חיצוניים, מחיקה וייחוס ל־TMDB, JustWatch, Spotify ו־Deezer.
- תוקן מרוץ אתחול: חלון Spotify הנסתר נוצר רק לאחר שה־renderer הראשי מוצג, כדי שהחלון הנגיש והנבדק הראשון יהיה תמיד ממשק OmniFlux.

### קבצים שנוצרו

- `src/renderer/src/assets/alexandre-heymann.svg`
- `src/renderer/src/assets/tmdb-approved.svg`
- `src/renderer/src/components/LegalModal.tsx`

### קבצים ששונו

- Playback ו־IPC: `src/renderer/src/store/player.ts`, `src/renderer/src/components/Controls.tsx`, `src/renderer/src/components/MiniPlayer.tsx`, `src/renderer/src/App.tsx`, `src/renderer/src/lib/activate.ts`, `src/shared/api.ts`, `src/preload/index.ts`, `src/main/index.ts`.
- UI: `src/renderer/src/screens/NowPlaying.tsx`, `src/renderer/src/screens/Music.tsx`, `src/renderer/src/screens/Settings.tsx`, `src/renderer/src/components/Sidebar.tsx`, `src/renderer/src/components/Poster.tsx`.
- Drive / metadata: `src/legacy_services/titles/index.ts`, `src/main/services/hub.ts`, `src/core/metadata/index.ts`, `src/legacy_services/__tests__/titles.spec.ts`.
- תרגומים: כל שמונת קובצי `src/shared/i18n/locales/*.ts`.
- תיעוד: `CHANGELOG_DEV.md`.

### קבצים שנמחקו

- לא נמחקו קבצים.

### בדיקות ואימות

- `npm test` — **238 passed**, **2 skipped**, 0 failed מתוך 240. שני הדילוגים הם בדיקות Deezer חיות התלויות בזמינות רשת/תוכן חיצוני.
- `npm run typecheck` — עבר ללא שגיאות TypeScript.
- `npm run build` — עבר; main, preload, renderer הראשי ו־Spotify renderer המבודד נבנו בהצלחה.
- `npm run verify:contrast` — **140 אלמנטים**, 0 מתחת ל־WCAG AA; היחס הנמוך ביותר `5.12:1`.
- `npm run verify:shots` — עבר עבור Home, Music, Library, Settings ו־Search בצרפתית LTR, עברית RTL וערבית RTL. נבדקו גם badge היוצר והיפוך ה־Sidebar חזותית.
- `git diff --check` — נקי; אזהרות LF/CRLF בלבד בסביבת Windows.
- לא בוצעה בדיקת Spotify Premium חיה מקצה לקצה, משום שהיא דורשת חשבון Premium והרשאת OAuth אינטראקטיבית שאינם זמינים בריצת CI. מסלול Widevine/Web Playback, החלפת האסימונים, ה־IPC, המצב האופטימי וה־fallback אומתו בבנייה ובבדיקות האוטומטיות.

---

## 2026-08-24 — Official Brand Asset Correction

- קובץ היוצר הרשמי שהיה בשורש הפרויקט בשם `ChatGPT Image 24 août 2026, 08_38_54.png` הועבר ושמו שונה ל־`src/renderer/src/assets/alexandre-heymann-official.png`.
- שחזור ה־SVG הלא־רשמי `src/renderer/src/assets/alexandre-heymann.svg` נמחק. Settings וה־Sidebar מציגים כעת את תמונת המקור הרשמית ללא ציור מחדש, על משטח בהיר השומר על צבעי המותג המקוריים.
- קונספט OmniFlux המאושר `docs/brand/omniflux-mark-concept-v1.png` הפך למקור האמת עבור סימן הממשק, `build/icon.png`, אייקון האתר ותצוגות 256/64/32/16 פיקסלים.
- `src/renderer/src/components/Mark.tsx` אינו מצייר עוד וריאציה וקטורית נפרדת, אלא מציג את נכס המותג הקנוני.
- `tools/_makeicon.mjs` שוכתב כך שכל הרצה עתידית תפיק את אייקוני Windows/Installer/Web מאותו קונספט ולא תחזיר בטעות את הסמל הישן.

### קבצים שנוצרו / שונו / נמחקו

- נוצרו: `src/renderer/src/assets/alexandre-heymann-official.png`, `src/renderer/src/assets/omniflux-mark.png`.
- שונו: `src/renderer/src/components/Mark.tsx`, `src/renderer/src/components/Sidebar.tsx`, `src/renderer/src/screens/Settings.tsx`, `tools/_makeicon.mjs`, `build/icon.png`, `build/_icon-preview/*.png`, `site/assets/icon.png`, `CHANGELOG_DEV.md`.
- נמחק: `src/renderer/src/assets/alexandre-heymann.svg`.

### אימות

- `npm run typecheck` — עבר.
- `npm run build` — עבר, ושני נכסי המותג נכללו ב־bundle.
- `src/main/__tests__/windows-shell.spec.ts` — עבר; חלון Windows ממשיך להשתמש ב־`build/icon.png` הממותג.
- `npm run verify:shots` — עבר בצרפתית LTR, עברית RTL וערבית RTL; שני הלוגואים נבדקו חזותית ב־Sidebar.
- `npm run verify:contrast` — 140 אלמנטים, 0 מתחת ל־WCAG AA; יחס מינימלי `5.08:1`.
- `git diff --check` — נקי; אזהרות LF/CRLF בלבד.

---

## 2026-08-24 — Embedded Spotify, Private Drive Streaming & Instant Transport

### תוצאה פונקציונלית

- OmniFlux נרשם כעת כהתקן Spotify Web Playback פנימי בשם `OmniFlux Player`. מסך המוזיקה אינו תלוי עוד בכך שאפליקציית Spotify אחרת תהיה פתוחה, ואינו מציג ספירה מטעה של התקנים חיצוניים.
- פקודות Pause, Resume, Seek, Previous, Next ו־Volume נשלחות קודם ישירות למופע ה־Web Playback SDK המבודד. קריאות Web API נשמרו כנתיב גיבוי רק כאשר פקודת SDK מקומית אינה זמינה.
- ניגון Spotify מפעיל URI מלא על ההתקן הפנימי לאחר העברה/אקטיבציה שלו. שגיאת race ראשונית של התקן חדש מטופלת באמצעות transfer וניסיון חוזר קצר.
- fallback אוטומטי ושקוף מ־Spotify ל־Deezer הוסר מנתיב ההפעלה: כשל Premium או SDK מוצג למשתמש באופן מפורש יחד עם כפתורי הרשמה רשמיים. תוצאות Deezer ייעודיות ממשיכות להתנגן בתוך OmniFlux ומסומנות בבירור כתצוגות מקדימות של 30 שניות.
- מקור Google Drive פרטי משתמש כעת ב־OAuth Authorization Code עם PKCE, loopback מקומי, scope קריאה בלבד ורענון token. כתובת המדיה היא `/drive/v3/files/{fileId}?alt=media`; אסימון ה־Bearer נמסר ל־mpv באמצעות `http-header-fields`, נשמר עבור בקשות Range, ואינו מוכנס ל־URL, לקטלוג או למצב ה־renderer.
- לחיצה על כרטיס Drive ממשיכה לפתוח את חלון פרטי הכותר המועשר מ־TMDB. פעולת `Regarder` פותרת את קובץ Drive בזמן אמת ומטעינה אותו ב־mpv עם הרשאת המשתמש.
- Play/Pause, Seek ו־Skip מעדכנים את Zustand באופן סינכרוני לפני IPC. מוני גרסה מגינים מפני תשובות engine ישנות, כך שהאייקון וה־timeline מגיבים בפריים הלחיצה ואינם קופצים לאחור.
- Local ו־Drive משתמשים באותו מנוע mpv ולכן שומרים על האצת חומרה, פילטרי וידאו, כתוביות, החלפת רצועות אודיו וסנכרון. Spotify משתמש במנוע Widevine/Web Playback המבודד; Deezer הציבורי נשאר preview בלבד ולא מוצג כשיר מלא.

### מיתוג היוצר

- נוצר `alexandre-heymann-dark.png` מתוך נכס המקור הרשמי: רקע RGBA שקוף אמיתי, כיתוב ומונוגרמה כהים הומרו ל־Arctic White (`#F5F7FA`), והדגשות cyan נשמרו.
- נכס ה־ImageGen שעבר ניסיון ניקוי לא נבחר ל־UI משום שהכיל checkerboard מוטמע ב־RGB. הנכס הסופי הופק בניקוי דטרמיניסטי של המקור כדי לשמור במדויק על הגאומטריה, הטקסט והצבעים הרשמיים.
- Settings / About ו־Sidebar footer מציגים את אותו נכס כהה וללא משטח לבן. אימות PNG אישר `Format32bppArgb` ו־alpha שקוף בפינות.

### קבצים שנוצרו

- `src/main/services/google-drive-auth.ts` — OAuth PKCE, loopback, שמירת token ורענון Google Drive.
- `src/adapters/source-gdrive/__tests__/gdrive.spec.ts` — כיסוי Bearer אסינכרוני, `alt=media` ובידוד token.
- `src/main/__tests__/playback-architecture.spec.ts` — כיסוי סדר optimistic-before-IPC, Drive PKCE ו־RGBA של תג היוצר.
- `src/renderer/src/assets/alexandre-heymann-dark.png` — תג היוצר המותאם לקנבס Obsidian.

### קבצים ששונו בסבב זה

- Spotify ו־OAuth: `src/renderer/src/spotify-player.ts`, `src/main/services/spotify-web-playback.ts`, `src/main/services/spotify-connect.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/shared/api.ts`, `src/main/__tests__/spotify-music.spec.ts`.
- Drive ו־mpv: `src/adapters/source-gdrive/index.ts`, `src/main/services/library.ts`, `src/main/engine/mpv.ts`.
- Playback UI: `src/renderer/src/store/player.ts`, `src/renderer/src/lib/activate.ts`, `src/renderer/src/components/MiniPlayer.tsx`, `src/renderer/src/components/Controls.tsx`, `src/renderer/src/App.tsx`, `src/renderer/src/screens/Music.tsx`.
- Branding: `src/renderer/src/components/Sidebar.tsx`, `src/renderer/src/screens/Settings.tsx`.
- תיעוד: `CHANGELOG_DEV.md`.

### קבצים שנמחקו

- לא נמחקו קבצי פרויקט בסבב זה. נכס המקור `alexandre-heymann-official.png` נשמר לצד גרסת dark לצורכי מקור והשוואה.

### אימות ובדיקות

- `npm test` — **245 passed**, **2 skipped**, 0 failed מתוך 247. שני הדילוגים הם תרחישי Deezer חיים התלויים בשירות חיצוני.
- `npm run typecheck` — עבר ללא שגיאות TypeScript.
- `npm run build` — עבר; main, preload, renderer הראשי ו־Spotify renderer המבודד נבנו בהצלחה.
- `npm run verify:contrast` — **140 אלמנטים**, 0 מתחת ל־WCAG AA; היחס הנמוך ביותר `5.12:1`.
- `npm run verify:shots` — עבר ב־15 מסכים: Home, Music, Library, Settings ו־Search בצרפתית LTR, עברית RTL וערבית RTL. תג היוצר השקוף נבדק חזותית ב־Settings וב־Sidebar.
- בדיקות engine קיימות אישרו ניגון mpv, `d3d11va`, Pause/Resume, Seek, thumbnail sheet, רצועות אודיו, פילטרים, delay, aspect ratio, PiP ומסך מלא.
- לא בוצע E2E חי של Spotify מלא או קובץ Drive פרטי משום שהם דורשים חשבון Premium/Google והרשאת OAuth אינטראקטיבית. החוזים, IPC, PKCE, כותרי Bearer, SDK transport, build והנתיבים האופטימיים מכוסים אוטומטית.

---

## 2026-08-24 — Global Account Hub & Licensed Multi-Service Playback

### Account Hub ומנויים

- אזור Spotify הנפרד ב־Settings הוחלף ב־Account Hub אחיד עבור Spotify, Deezer ו־Google Drive. כל שורה מציגה מצב הגדרה, חיבור, שם חשבון כאשר הספק מחזיר אותו, tier וכפתורי Connect/Disconnect.
- Spotify קורא את `/v1/me` תחת `user-read-private` וממפה `product` ל־Premium או Free. ה־tier ושם התצוגה נשמרים ב־`spotify.json` לצד OAuth session ומתעדכנים מהספק.
- Deezer שומר מקומית רק tier ושם תצוגה ב־`deezer-account.json`; סשן ה־SDK נשמר במחיצת Chromium ייעודית `persist:omniflux-deezer`. האפליקציה אינה מבקשת או שומרת סיסמת Deezer.
- Google Drive קיבל פעולות חיבור וניתוק מפורשות ב־IPC וב־Settings, תוך שמירה על PKCE, loopback ו־`drive.readonly` בלבד.
- נוסף `.env.example` המתעד את `MAIN_VITE_DEEZER_APP_ID` ואת מזהי OAuth הציבוריים בלי לכלול tokens או סודות סביבת משתמש.

### Spotify playback

- נשמרו scopes החובה `streaming`, `user-read-email`, `user-read-private`, `user-read-playback-state` ו־`user-modify-playback-state`, לצד scopes של Music Hub שכבר היו קיימים.
- סדר ההפעלה הוקשח: כל רצועה מעבירה תחילה את ההשמעה במפורש ל־`device_id` הפנימי באמצעות `PUT /v1/me/player`, ורק לאחר הצלחת ההעברה נשלחת בקשת `/me/player/play?device_id=...`.
- ניסיון חוזר של 150ms נשמר רק לחלון propagation של התקן חדש; אין בחירה או תלות בהתקן Spotify חיצוני.
- `/v1/me` משמש גם לזיהוי Free/Premium. Free מוצג במרכז החשבונות ואינו מוצג כאילו Web Playback יכול לנגן בו רצועה מלאה.

### Deezer SDK והגבלת entitlement

- נוסף renderer מבודד ל־Deezer JavaScript SDK עם `DZ.init`, `DZ.login` ו־`DZ.player.playTracks`. חלון הרקע משתמש ב־sandbox, ללא Node, ללא Taskbar ובמחיצה מתמשכת נפרדת.
- כל תוצאת Deezer נושאת `providerTrackId` אטום. Premium מנותב ל־SDK בלי לחשוף URL של stream מלא ל־renderer או ל־mpv.
- Play/Pause/Resume/Seek מחוברים ל־SDK. Pause מבצע fade קצר של 72ms למניעת click/pop; Resume מפעיל מיד ומחזיר את עוצמת הקול.
- המצב האופטימי הורחב ל־Deezer: לחיצת Premium מעדכנת מיד עטיפה, כותר, ספק ומצב Play לפני השלמת קריאת ה־SDK; polling מסנכרן את מצב האמת כל 500ms.
- בהתאם לטבלת הזכויות הרשמית של Deezer, Music on Demand בחשבון Free או לא מחובר נשאר preview של 30 שניות. רק Premium מסומן `Titre complet`. לא הוטמע מסלול Free מלא עם פרסומות משום שה־SDK הציבורי אינו מעניק entitlement כזה לטרק לפי דרישה.
- נוסף `deezer-mark.svg` כנכס מקומי קבוע, ו־MusicProviderBadge אינו תלוי עוד בנתיב תמונה חסר או ב־SVG inline עם מזהה gradient משותף.

### Google Drive, lyrics, branding ו־Legal

- ניגון Drive ממשיך להשתמש ב־`alt=media` עם Bearer בכותר mpv, לרבות Range requests; כרטיס Drive ממשיך לפתוח פרטי TMDB ואת פעולת `Regarder`.
- מנגנון LRC הקיים נשמר: pane ממורכז, שורה פעילה לבנה, שורות סביב ב־`#6E7480` ו־`scrollIntoView({ behavior: 'smooth', block: 'center' })`.
- תג `Built by Alexandre Heymann` השקוף נשמר ב־Settings/About וב־Sidebar.
- נוסחי Privacy בכל שמונה השפות עודכנו כדי לתאר Spotify OAuth, Google Drive OAuth, סשן Deezer SDK ואפשרות ניתוק עצמאית מכל שירות.

### קבצים שנוצרו

- `.env.example`
- `src/main/services/deezer-web-playback.ts`
- `src/renderer/deezer-player.html`
- `src/renderer/deezer-channel.html`
- `src/renderer/src/deezer-player.ts`
- `src/renderer/src/assets/deezer-mark.svg`
- `src/main/__tests__/account-hub.spec.ts`

### קבצים ששונו

- SDK/build: `electron.vite.config.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/shared/api.ts`.
- Spotify: `src/main/services/spotify-auth.ts`, `src/main/services/spotify-connect.ts`.
- Deezer/search: `src/adapters/provider-deezer/index.ts`, `src/core/search/types.ts`, `src/renderer/src/lib/activate.ts`, `src/renderer/src/store/player.ts`.
- UI: `src/renderer/src/screens/Settings.tsx`, `src/renderer/src/screens/Music.tsx`, `src/renderer/src/components/MusicProviderBadge.tsx`.
- i18n/Legal: כל שמונת קובצי `src/shared/i18n/locales/*.ts`.
- תיעוד: `docs/SPOTIFY_BENCHMARK.md`, `CHANGELOG_DEV.md`.

### קבצים שנמחקו

- לא נמחקו קבצים.

### בדיקות ואימות

- `npm test` — **250 passed**, **2 skipped**, 0 failed מתוך 252. שני הדילוגים הם תרחישי Deezer חיים התלויים ברשת ובתוכן חיצוני.
- `npm run typecheck` — עבר ללא שגיאות.
- `npm run build` — עבר; נארזו main, preload, renderer ראשי, Spotify player, Deezer player ו־Deezer channel.
- `npm run verify:contrast` — **147 אלמנטים**, 0 מתחת ל־WCAG AA; יחס מינימלי `5.12:1`.
- `npm run verify:shots` — עבר ב־15 מסכים בצרפתית LTR, עברית RTL וערבית RTL. Account Hub, quick-auth, תג היוצר ולוגו Deezer נבדקו חזותית.
- נוספו חמש בדיקות ארכיטקטורה: Spotify scopes/tier, transfer-before-play, בידוד Deezer/entitlement, Google Drive account controls וניתוב Premium/preview.
- לא בוצע E2E חי מול Spotify Premium, Deezer Premium או Drive פרטי מפני שהוא דורש חשבונות והרשאות OAuth אינטראקטיביות. נתיבי הקוד, חוזי IPC, בניית ה־SDK surfaces וההתנהגות ללא credentials אומתו אוטומטית.

---

## 2026-08-24 — תיקוני ספקי מוזיקה, Spotify מוטמע ו־Google Drive

### תיקוני UI וסינון ספקים

- הוסר כפתור `S’abonner à Deezer` הכפול מאזור Spotify. פעולת המנוי ל־Deezer מופיעה פעם אחת בלבד, בשורת החשבון של Deezer.
- הנכס `src/renderer/src/assets/deezer-mark.svg` הוחלף בסימן Deezer הרשמי (לב/גלי קול סגולים) כנכס מקומי חד, ללא תלות בכתובת רשת בזמן ציור הממשק.
- תוצאות ושורות מוזיקה מסוננות לפי חיבור החשבון: Spotify מוצג רק כאשר Spotify מחובר, ו־Deezer רק לאחר התחברות Deezer. קבוצות סרטים/סדרות, ספרייה מקומית ו־Google Drive אינן מושפעות מהסינון.
- מצב החיבור של Deezer נשמר ומבוטל במפורש ב־connect/disconnect, ורענון ה־Hub מתבצע עם שינוי חשבון.

### Spotify Web Playback

- תוקן CSP של משטח Spotify המבודד כך שה־iframe והחיבורים של `sdk.scdn.co` מורשים.
- אתחול CastLabs Widevine (`components.whenReady`) מועבר לפני יצירת ה־BrowserWindow הראשון, כנדרש לרישום EME בחלונות Chromium.
- בדיקת E2E חיה מול חשבון Spotify Premium המחובר עברה מחוץ ל־Playwright (מצב Playwright מפעיל Electron עם `--no-sandbox`, שמנטרל DRM): OmniFlux נרשם כמכשיר `OmniFlux Player`, `Golden` נותב אליו, ו־Spotify החזיר `ok: true` ומצב `active: true`.

### Google Drive

- נוסף `src/main/services/drive-stream.ts`: גשר HTTP מקומי הקשור ל־`127.0.0.1` בלבד, עם נתיב אקראי קצר־חיים. אסימון Google OAuth נשאר בתהליך הראשי ואינו נכנס ל־renderer, לכתובת הקטלוג או למצב mpv.
- הגשר תומך ב־Range ומקטעים עבור mpv, כולל retry לתשובות זמניות, ונסגר בעת יציאת האפליקציה.
- נוספה בדיקת זמינות לפני פתיחת הנגן. `downloadQuotaExceeded`, הרשאה חסרה ותקלת רשת מוצגות כהודעה ברורה במקום מסך שחור; נוספו תרגומים לכל שמונה השפות.
- בחשבון המחובר שנבדק Google החזיר בפועל `downloadQuotaExceeded` לקבצי Drive הנוכחיים. לכן ספק Google חסם את הניגון של אותם קבצים בצד השרת; האפליקציה כעת מדווחת זאת במפורש ומציעה לנסות מאוחר יותר או ליצור עותק פרטי. קובץ זמין ממשיך לנתיב הזרמת `alt=media` המאומת.

### קבצים שנוצרו

- `src/main/services/drive-stream.ts`.

### קבצים ששונו בסבב זה

- חשבונות/סינון: `src/main/index.ts`, `src/main/services/deezer-web-playback.ts`.
- Spotify: `src/renderer/spotify-player.html`, `src/main/services/spotify-web-playback.ts`.
- Drive/mpv: `src/main/services/library.ts`, `src/main/services/drive-stream.ts`, `src/main/engine/mpv.ts`.
- UI: `src/renderer/src/screens/Music.tsx`, `src/renderer/src/lib/activate.ts`, `src/renderer/src/store/player.ts`, `src/renderer/src/components/Poster.tsx`, `src/renderer/src/screens/TitleDetails.tsx`.
- מיתוג: `src/renderer/src/assets/deezer-mark.svg`, `src/renderer/src/components/MusicProviderBadge.tsx`.
- i18n: כל שמונת קובצי `src/shared/i18n/locales/*.ts`.
- בדיקות: `src/adapters/source-gdrive/__tests__/gdrive.spec.ts`, `src/main/__tests__/playback-architecture.spec.ts`.
- תיעוד: `CHANGELOG_DEV.md`.

### קבצים שנמחקו

- לא נמחקו קבצי פרויקט.

### בדיקות ואימות

- `npm test` — **252 passed**, **2 skipped**, 0 failed מתוך 254.
- `npm run typecheck` — עבר ללא שגיאות TypeScript.
- `npm run build` — עבר; main, preload, renderer, Spotify player ו־Deezer player נבנו בהצלחה.
- `npm run verify:contrast` — **144 אלמנטים**, 0 מתחת ל־WCAG AA; יחס מינימלי `5.08:1`.
- `npm run verify:shots` — עבר ב־15 מסכים בצרפתית LTR, עברית RTL וערבית RTL.
- `git diff --check` — עבר ללא שגיאות whitespace (אזהרות CRLF בלבד).

---

## 2026-08-24 — ייצוב החלפת רצועות Spotify ואבחון אובדן שמע

- אותר ותוקן תור מצטבר: כל לחיצה על שורה הוסיפה קודם עד עשר רצועות באמצעות `/me/player/queue`, ולכן שירים מבחירות קודמות נשארו בתור ויכלו להחליף את השיר שנבחר. כעת השיר והרצף הנראה אחריו נשלחים אטומית כ־`uris` בבקשת Start/Resume אחת, ללא לולאת `addToQueue`.
- נוספו מאזיני `player_state_changed` ו־`playback_error` למשטח Spotify המבודד, יחד עם מצב בריאות פנימי הכולל URI, זמן, pause ושגיאת SDK.
- בדיקת נגינה חיה שחזרה את התקלה הוכיחה שהאודיו עצמו נטען בהצלחה (`206` מ־Spotify CDN), אך שרת הרישיונות `widevine-license` של Spotify מחזיר שוב ושוב `HTTP 500`; לאחר מכן ה־SDK פולט `Playback error`. זהו מקור ההיעלמות לאחר כעשר שניות, ולא timer או pause של OmniFlux.
- נוספה הגנה מפני השתקה שקטה: לאחר התחלת נגינה OmniFlux בודק את בריאות ה־SDK. אם רישיון DRM נדחה, המכשיר המוטמע מסומן כלא זמין. אם קיים מכשיר Spotify רשמי אחר, ההשמעה מועברת אליו אוטומטית; אחרת מוצגת הודעה מפורשת על צורך בבניית Electron חתומה VMP לייצור.
- מצב הנגן והפקדים ממשיכים לעקוב אחר מכשיר fallback חיצוני כאשר המשטח המוטמע נפסל.
- לפי תיעוד CastLabs, הבינאריים המוכנים חתומים ל־VMP פיתוח בלבד; שירות DRM יצרני עשוי לדרוש חתימת EVS/VMP לייצור. סביבת העבודה אינה כוללת כרגע `castlabs_evs`, ולכן לא ניתן היה ליצור חתימת ייצור בסבב זה.

### קבצים ששונו

- `src/renderer/src/screens/Music.tsx`
- `src/renderer/src/lib/activate.ts`
- `src/renderer/src/spotify-player.ts`
- `src/main/services/spotify-connect.ts`
- `src/main/services/spotify-web-playback.ts`
- `src/main/index.ts`
- `src/preload/index.ts`
- `src/shared/api.ts`
- כל שמונת קובצי `src/shared/i18n/locales/*.ts`
- `src/main/__tests__/spotify-music.spec.ts`
- `CHANGELOG_DEV.md`

### בדיקות

- `npm run typecheck` — עבר.
- `npm run build` — עבר; כל משטחי Electron וה־renderer נבנו בהצלחה.
- `npm test` — **253 passed**, **2 skipped**, 0 failed מתוך 255.
- בדיקות Spotify + Account Hub + i18n ממוקדות — **44 passed**, 0 failed.
- E2E חי: שוחזר `HTTP 500` מ־Spotify Widevine License ו־`Playback error`, תוך אימות שמקטעי האודיו עצמם חזרו `206`.

---

## 2026-08-24 — בדיקת מוכנות Git והפצה

### שינויי אריזה

- עודכן `electron-builder.yml` עם מראת ההפצות של CastLabs תחת
  `electronDownload`. השינוי מתקן שגיאת `404` שנגרמה כאשר גרסת
  `43.0.0+wvcus` המותאמת התבקשה משרת ההפצות הרשמי של Electron.
- נוצר מתקין NSIS מקומי חדש באמצעות `npm run dist -- --publish never`; לא
  בוצעה העלאה או פרסום חיצוני.
- עודכן `src/main/window/player.ts`: מצב Picture-in-Picture מחיל מיקום וגודל
  בפעולת `BrowserWindow.setBounds` אטומית אחת, כדי למנוע מ-Windows להשאיר את
  החלון ברוחב הרגיל כאשר קריאות שינוי גודל ומיקום נפרדות מתמזגות.

### אימות וחסמי הפצה

- `npm run typecheck` — עבר.
- `npm test` — בריצה הראשונית: **252 passed**, **2 skipped**, **1 failed**;
  הכשל היה רגרסיית גבולות חלון PiP שתוקנה בסבב זה.
- `npx playwright test tests/power.spec.ts:146` לאחר התיקון — **1 passed**.
- `npm run dist -- --publish never` — עבר ויצר מקומית את
  `release/OmniFlux-2.0.0-alpha.3-setup.exe`.
- בדיקת החבילה (`node tools/_packagecheck.mjs`) — נכשלה: החבילה לא הפעילה את
  מנוע mpv ולא טענה את קובץ הבדיקה. ארכיון CastLabs מכיל `electron.exe.sig`,
  בעוד electron-builder משנה וממתג את קובץ ההפעלה. נדרשים credentials ו-hook
  לחתימת EVS/VMP לאחר האריזה לפני שבניית Widevine תהיה כשירה להפצה.
- בדיקת Windows Authenticode — המתקין וקובץ האפליקציה מדווחים `NotSigned`.
  נדרשת תעודת code-signing אמינה לפני פרסום ציבורי.
- בדיקת Git — הקובץ `.env` מוחרג ואינו במעקב. קיימים 101 נתיבים ששונו או
  נוספו, ולכן לא בוצעו commit או push רחבים ללא אישור מפורש על ההיקף.
